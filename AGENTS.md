# AGENTS.md — دليل المهام للجلسات القادمة (Task Playbook)

> هذا الملف موجّه لأي جلسة تطوير (إنسان أو AI agent) تكمل لعبة ROK2. اقرأه أولاً قبل أي عمل.

---

## 1. سياق المشروع في 30 ثانية

- **اللعبة:** ROK2 — لعبة استراتيجية MMO شبيهة Rise of Kingdoms.
- **العميل الوحيد:** Unreal Engine 5 (C++ module `Rok2`) في `game/client-unreal/` — PC + Android. **لا يوجد عميل ويب — حُذف `game/client/` نهائياً.**
- **الخادم:** Cloudflare Workers + D1 + Durable Objects في `game/backend/` — مباشر على `https://rok2-api.lolelarap.workers.dev`.
- **مصدر الحقيقة للتقدم:** `PLAN.md` في الجذر — لا تبدأ قبل قراءته. **حالة 2026-08-12: المراحل 1–6 مكتملة `[x]`؛ المرحلة النشطة الآن هي 7 (الجاهزية التشغيلية وأول موسم قابل للّعب). أُغلق P7-T4 بدليل موسم Worker/D1 محلي معزول كامل. ربط P7-T1، بوابة P7-T2، تجهيز محتوى P7-T9، ومسارا P7-T3 الخادمي وP7-T5 لاستعادة الاتصال، وخط أساس P7-T6 لأداء العالم اجتازوا فحوصاً آلية أو محلية. تتوفر الآن `Docs/PIE_TWO_CLIENTS_ANDROID_ACCEPTANCE.md` و`Prepare-AndroidDevelopment.ps1` و`test:pie-android-acceptance` للتحضير المكرر؛ لكن البنود المرتبطة بالعميل تبقى `[ ]` إلى أن توثق دورة PIE على UE 5.4.4 وقياس Android المستهدف عند الاقتضاء. لا تُعِد تنفيذها، بل احجز تحقق PIE/Android أو أول بند P7 غير مملوك.**
- **مصدر الحقيقة للتصميم:** `07-game-design/` — GDD + قلعة سداسية + حضارات بصرية + RoK audit + UI/UX + توازن + صور مرجعية في `assets/`.
- **الفرع الوحيد:** `main` — كل التسليمات عليه مباشرة.

## 2. بروتوكول كل جلسة (إلزامي)

1. **اقرأ `PLAN.md`** وحدد أول بند `[ ]` غير مكتمل في المرحلة الحالية.
2. **أعلن** أي بند (أو مجموعة بنود مترابطة) ستنفذه قبل البدء.
3. **نفّذ** البند: كود + اختبار عملي قدر الإمكان.
4. **حدّث `PLAN.md`** في نفس الـ commit:
   - علّم البند `[x]`
   - أضف سطراً في جدول "سجل الإنجاز" (التاريخ، البند، ملاحظات)
5. **Commit على `main`** برسالة واضحة بالصيغة: `P<n>-T<n>: <وصف قصير>` ثم push ونشر فوري.
6. **إذا لم تكتمل المهمة:** لا تعلّمها `[x]` — دوّن ما توقفت عنده في سجل الإنجاز كـ "WIP".

> **ملاحظة عملية:** الجلسات الآلية المتسلسلة قد تنجز عدة بنود مترابطة في دفعة واحدة (سجل 2026-07-28 مثال: P1–P5 في يوم واحد). المهم أن يكون كل بند مُعلناً ومُختبراً وموثّقاً في `PLAN.md` — لا تُخفِ بنداً ثانياً داخل كوميت بندٍ أول.

## 3. قواعد كتابة الكود

- **لا قيم توازن ثابتة في الكود** — اقرأ من `data/*.json` أو من API يخدمها.
- **الخادم هو السلطة** (server-authoritative) — العميل لا يحسب نتائج قتال أو موارد.
- **توافق المحرك:** نسخة البناء المعتمدة هي **UE 5.4.4**؛ `Rok2.uproject` مرتبط بسلسلة `5.4` وسكربتات البناء/الاستيراد/الدخان تتحقق من `Engine\Build\Build.version`. لا تستخدم API خاصاً بإصدار أحدث من 5.4.4 دون مسار احتياطي موثق.
- ملفات C++ الجديدة تتبع نفس نمط الموجود: header في `Public/`، تنفيذ في `Private/`، تسجيل في `Rok2.Build.cs` عند الحاجة.
- **أي عمل بصري/واجهة يبدأ من `07-game-design/`** — GDD ومواصفة القلعة السداسية والحضارات و UI/UX والصور في `assets/` هي المرجع الملزم للشكل المستهدف. لا تخترع أسلوباً بصرياً من رأسك.

## 4. الاختبار قبل كل تسليم

- Backend: `cd game/backend && ADMIN_KEY=... npm run smoke` ضد البيئة الحية (المفتاح من البيئة — لا قيمة افتراضية).
- Client: بناء المشروع دون أخطاء تجميع + اختبار PIE سريع إن أمكن. لقبول عميلين أو Android، نفّذ أولاً `Prepare-AndroidDevelopment.ps1` على Windows/UE 5.4.4، واتبع `Docs/PIE_TWO_CLIENTS_ANDROID_ACCEPTANCE.md`؛ لا يتحول نجاح السكربت أو APK إلى قبول ميداني بلا سجل الأدلة.
- أي تغيير في الـ API يستلزم تحديث `game/docs/API.md` في نفس الـ commit.

## 5. العمل المتوازي متعدد الشاتات

> المشروع يدعم تشغيل عدة جلسات Manus بالتوازي داخله. المرجع الكامل: `WORKFLOW_MULTI_TASK.md` في ملفات المشروع المشتركة.

- **نطاق الملفات مقدس:** كل جلسة تعمل على نطاق محدد معلن في أول رسالتها، ولا تعدّل ملفات خارجه (تُقرأ فقط).
- **احجز بندك شفهيًا:** «سأنفذ P6-T8» في أول رسالة — حتى تعرف الجلسات الأخرى نطاقك.
- **آمن بالتوازي:** توليد الأصول (أصوات/أيقونات/صور/موديلات — ملفات جديدة فقط)، سكربتات التحقق، وبنود متباعدة النطاق.
- **تسلسلي:** تعديلات backend/C++ المشتركة والأنظمة المترابطة — بند بند حسب `PLAN.md`.
- **git:** اسحب آخر `main` قبل push؛ عند تعارض **لا تُحل التعارض بنفسك** — بلّغ المالك.

## 6. أولويات ثابتة عند التعارض

1. استقرار الاتصال client ↔ server
2. صحة حالة اللعبة على السيرفر
3. تجربة اللاعب المرئية (خرائط/واجهات)
4. الأصول الفنية (آخر شيء — placeholder مقبول دائماً)

## 7. ما لا يجب فعله

- ❌ لا تبدأ نظاماً جديداً (قادة/بحث/مواسم) قبل اكتمال بوابة المرحلة الحالية.
- ❌ لا تعدّل `wrangler.jsonc` أو الأسرار الحقيقية أو تغيّر الـ API Base URL.
- ❌ لا تنشئ فروعاً أو PRs — التسليم مباشر على `main` (قرار مالك المشروع)، إلا في العمل المتوازي الكبير فاستخدم فرع `parallel/<البند>` وتُدمج من جلسة تجميع. *استثناء 2026-07-29: دُمج برانشا الإصلاح عبر PR لحفظ التاريخ، ثم عاد العمل على main.*
- ❌ في العمل المتوازي لا تعدّل ملفات جلسة أخرى ولو كان التعديل صغيرًا — اطلب منها أو بلّغ المالك.
- ❌ لا تترك التوثيق متناقضاً — إذا غيّرت سلوكاً، حدّث الملف الموثق له.
- ❌ لا تلتزم ملفات مؤقتة/مستخرجة (tmp_*، *_extracted، *.apk) — راجع `.gitignore`.

---

*آخر تحديث: 2026-08-12 — P7-T8 في WIP: فُرضت هوية اللاعب الموثقة داخل `KingdomShard` على أوامر المدينة والمسيرات والطوابير والكشافة، مع تحقق داخلي لمفتاح الإدارة في `/admin`، وحفظ موسم لا يستبدل `season_start_ms`، ودردشة تحالف معزولة، وتقارير قتال مرئية للمدافع وتحالفه. لأي تعديل في `KingdomShard.ts` أو `router.ts` أو `Rok2Api.cpp` شغّل `npm run test:security-hardening-contract` و`npm run check`. لا تُغلق P7-T8 قبل إضافة مؤشرات صحة Durable Objects وأخطاء الأوامر وزمن tick والطوابير المتعثرة وrunbook تشغيلي. P7-T6 في WIP: أضيفت لقطة `ARok2WorldRenderer::GetPerformanceSnapshot()` تقيس عناصر HISM وactors والمسبح ونافذة زمن الراسم عبر `URok2Perf`، مع `ResetPerformanceSnapshot()` وحارس `npm run test:world-performance-budget`. راجع `game/docs/P7_T6_WORLD_PERFORMANCE_BUDGET.md`، ولا تغلق البند قبل تشغيل Development مبني بـ UE 5.4.4 على جهاز Android مستهدف وتسجيل `stat unit` و`stat memory` و`stat RHI`. P7-T5 في WIP: تستعيد `URok2Api::RestoreAuthoritativeState()` المدينة والعالم والقادة والتقارير والراليات من المصدر السلطوي بعد عودة WebSocket حي سابق، وتمنع دورات التذبذب المتوازية وتلغي حالة الاستعادة عند الفصل اليدوي. شغّل `npm run test:reconnect-restore` لأي تعديل في `Rok2Api`، وراجع `game/docs/P7_T5_RECONNECT_RESTORE.md`. يلزم قبول PIE على Windows/UE 5.4.4 يثبت عدم ازدواج actors والعدادات قبل إغلاقه. أُغلق P7-T4 بدليل `game/backend/scripts/e2e_p7_t4_accelerated_season.mjs` ويشغّل عبر `npm run test:e2e-p7-t4` على Worker/D1 محليين نظيفين فقط؛ تفاصيل العزل وتصفير `.wrangler/state` في `game/docs/P7_T4_ACCELERATED_SEASON.md`. يثبت فتح الموسم والتعادل والتتويج من دون تغيير `dayMs` أو `maxDay` الإنتاجيين، كما يمرّر مسار إنشاء المسيرة `coreObjectiveId` إلى الخادم. تظل أعمال P7-T1/P7-T2/P7-T3/P7-T9 في WIP متحققة آلياً أو محلياً: أُغلقت فجوات الربط الثلاث في الكود، وأضيفت بوابة Windows للبناء والدخان (`Build-Rok2.ps1` و`Run-Rok2RuntimeSmoke.ps1`) وفحص `test:p7-t2-build-pipeline`، وحُوّلت شاشة الحضارات من ComboBox نصي إلى كاروسيل بصري. نسخة التشغيل المعتمدة الآن هي **UE 5.4.4** (`EngineAssociation: 5.4`)؛ ترفض سكربتات البناء والاستيراد والدخان أي `Build.version` آخر. توجد الخلفيات في `Content/Art/CivBackgrounds` والشعارات التشغيلية في `Content/Art/CivIcons` والقادة كـ PNG في `Content/Art/Commanders`؛ تُستورد عبر `Import-CivVisuals.ps1` وتُراجع عبر `test:p7-t9-civ-showcase`. أُضيفت كذلك حزمة المدينة والخريطة والواجهة: الأيقونات في `Content/Art/UIIcons`، جلود الأزرار في `Content/Art/UIButtons`، صور المباني في `Content/Art/CityBuildingIcons`، ونماذج Kenney CC0 في `Content/Art/KenneyCastleKit` مع الترخيص؛ الاستيراد عبر `Import-CityMapUIAssets.ps1` والتحقق عبر `test:city-map-ui-assets`. أضيف P7-T3 في `game/backend/scripts/e2e_p7_t3_player_journey.mjs` ويُشغَّل بـ`npm run test:e2e-p7-t3` مع `ADMIN_KEY` و`WAIT_FOR_RALLY=1` للوضع الكامل؛ أثبت محلياً رحلة لاعبين وتقاريرهما وعودة القوات. لا تتوفر أدوات Unreal هنا؛ على جهاز Windows مع UE 5.4.4 نفّذ اختبار عميلين وPIE قبل تعليم أي من البنود `[x]`.*
