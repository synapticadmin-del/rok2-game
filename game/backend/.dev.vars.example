# انسخ هذا الملف إلى .dev.vars للتطوير المحلي (.dev.vars مستثنى من Git).
# للنشر استخدم أسراراً حقيقية: wrangler secret put <NAME>
#
# ولّد قيماً عشوائية، لا تكتب كلمات مقروءة:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# الحد الأدنى للطول 24 حرفاً، والقيم المسرّبة القديمة مرفوضة صراحةً في
# src/lib/secrets.ts فلن تعمل حتى لو أُعيد ضبطها.

AUTH_SECRET="ضع-هنا-قيمة-عشوائية-طويلة"
ADMIN_KEY="ضع-هنا-قيمة-عشوائية-طويلة"
