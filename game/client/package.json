{
  "name": "rok2-client",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npx --yes serve public -l 5173",
    "deploy": "npx wrangler pages deploy public --project-name rok2-client --commit-dirty=true"
  }
}
