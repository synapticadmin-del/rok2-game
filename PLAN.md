# ROK2 — خطة التنفيذ الرئيسية (Master Execution Plan)

> **الهدف النهائي:** لعبة استراتيجية MMO كاملة بمستوى Rise of Kingdoms، عميلها الوحيد **Unreal Engine 5 (PC + Android)** وخادمها **Cloudflare Workers + D1 + Durable Objects**.
>
> **طريقة استخدام هذا الملف:** كل جلسة تطوير تبدأ بقراءة هذا الملف لتحديد أول بند غير مكتمل `[ ]`، تنفذه، ثم تحدّث هذا الملف نفسه (تعليم `[x]` + تدوين الملاحظات) وتعمل commit على `main`.
>
> **آخر تحديث:** 2026-08-12 — **P7-T1/P7-T2/P7-T9 قيد التحقق التشغيلي:** اكتمل ربط INT-01/02/03، وأضيفت بوابة بناء/دخان وتدفق اختيار حضارات بصري بأصول جاهزة للاستيراد. نسخة البناء المعتمدة أصبحت **UE 5.4.4**؛ لا تُغلق البنود حتى تمر دورة بناء وPIE على هذا الإصدار، إذ لا توجد أدوات Unreal في بيئة التنفيذ الحالية.

---

## 0. الحالة الحقيقية للمشروع (Ground Truth)

| المكوّن | الحالة | الملاحظات |
|---------|--------|-----------|
| التوثيق والتصميم | ✅ شبه مكتمل | خريطة 2400²، 6 حضارات، أنظمة أساسية موثقة في `01`–`06` + **وثائق التصميم الشاملة `07-game-design/`** (GDD، قلعة سداسية، حضارات بصرية، RoK audit، UI/UX، توازن) |
| بيانات JSON الموحدة | ✅ جاهزة | `data/*.json` (civilizations, zones, passes, buildings, troop_tiers, map_spec_coordinates) |
| Backend (Cloudflare) | 🟡 Prototype يعمل | Auth ضيف + مدينة + مبانٍ + جنود + تحالفات + ممرات + تقارير قتال + WebSocket. ناجح في smoke E2E |
| عميل UE5 (C++) | 🟡 هيكل أولي يعمل | موديول `Rok2` (Api, Camera, WorldRenderer, CityBuilder, Boot/City Widgets) — أشكال هندسية بدائية بدل الأصول الفنية |
| الأصول الفنية 3D/UI | 🟡 بدأت | مبانٍ/وحدات KayKit (P2-T7 ✅) + موسيقى ومؤثرات WAV لكل حضارة + 12 بورتريه قائد (P4-T2 ✅)؛ باقي: موديلات وحدات بشرية + أصول UI نهائية |
| الأنظمة المتقدمة | 🟡 بدأت | قادة (P2-T1 ✅) + مستشفى (P2-T2 ✅: جرحى بالسعة + شفاء زمني)؛ بحث جزئي (stubs)؛ لم تُنفّذ بعد: أحداث، مواسم، KvK، متجر، Battle Pass |
| الاختبار الآلي | ❌ محدود | smoke.mjs فقط على الـ backend |

**الخلاصة:** المشروع في نهاية المرحلة 1 (Prototype) وليس "مكتملاً 100%". أي تقرير يقول غير ذلك قديم.

---

## 1. قرارات معمارية مقفلة (لا تُعاد مناقشتها)

1. **العميل:** Unreal Engine 5 C++ فقط — لا عميل ويب. (الخلافات الموثقة حول 5.4/5.8 تُحسم: **الهدف أحدث UE5 مستقر 5.4+، والكود يجب أن يبني على أي منها**)
2. **الخادم:** Cloudflare Workers + D1 + Durable Object `KingdomShard` — Authoritative، tick ذري كل 1 ثانية.
3. **خريطة واحدة موسمية ثابتة بثلاث Zones** — لا KvK متعدد الخرائط قبل اكتمال الموسم الأول.
4. **كل بيانات التوازن تعيش في `data/*.json`** ويقرأها الطرفان (client + server) — لا قيم ثابتة مكررة في الكود.
5. **اللغة في التوثيق:** العربية أساسية مع مصطلحات تقنية إنجليزية.
6. **التسليم:** تعديل مباشر على `main` مع commit واضح + تحديث هذا الملف في نفس الـ commit.

---

## 2. خارطة الطريق التنفيذية (المراحل والبوابات)

### المرحلة 1 — إنهاء الـ Prototype ⬅️ *المرحلة الحالية*
الهدف: لاعبان حقيقيان على نفس السيرفر يتنازعان ممراً عبر عميل UE5 بدون شرح.

- [x] Backend: auth ضيف + مدينة + مبانٍ + تدريب + تحالف + ممرات + تقارير قتال
- [x] Backend: smoke E2E (`scripts/smoke.mjs`) ضد البيئة الحية
- [x] Client: اتصال REST + WebSocket بكلاس `URok2Api`
- [x] Client: كاميرا استراتيجية + رسم خريطة العالم + بناء مدينة أساسي
- [x] **P1-T1** توحيد نسخة المحرك والتوثيق (تنظيف تضارب 5.4/5.8 في كل الملفات) — ✅ تم 2026-07-28
- [x] **P1-T2** Client: شاشة تحميل + إعادة اتصال تلقائي + معالجة أخطاء الشبكة في `URok2Api` — ✅ تم 2026-07-28
- [x] **P1-T3** Client: مسيرات الجيوش مرئية على الخريطة (أيقونة متحركة من المدينة للهدف حسب زمن الـ march) — ✅ تم 2026-07-28
- [x] **P1-T4** Client: واجهة تقرير القتال (`URok2BattleReportWidget`) تعرض الخسائر من الـ API — ✅ تم 2026-07-28
- [x] **P1-T5** Client: شريط موارد حي يتحدث عبر WebSocket بدون polling يدوي — ✅ تم 2026-07-28
- [x] **P1-T6** Backend: endpoint لسحب بيانات `data/*.json` (buildings/civs/tiers) بدل القيم الثابتة — ✅ تم 2026-07-28
- [x] **P1-T7** اختبار لاعبين: سيناريو موثق في `game/docs/E2E_TWO_PLAYERS.md` + نجاحه فعلياً — ✅ تم 2026-07-28 (E2E TWO PLAYERS PASSED ضد الإنتاج)
- **🚪 بوابة النجاح:** ✅ **محققة 2026-07-28** — لاعبان يتنافسان على ممر ويستلمان تقارير قتال صحيحة (مثبت آلياً بـ e2e_two_players.mjs ضد الإنتاج).

### المرحلة 2 — Vertical Slice
- [x] **P2-T1** نظام القادة: بيانات `data/commanders.json` + عمولات/مهارات أساسية (attack/defense/passive واحدة لكل قائد) — ✅ تم 2026-07-28
- [x] **P2-T2** المستشفى: استقبال الجرحى حسب السعة + شفاء زمني عبر الـ tick — ✅ تم 2026-07-28
- [x] **P2-T3** شجرة البحث: Economic + Military tiers من `data/research.json` (إنشاء الملف) — ✅ تم 2026-07-28
- [x] **P2-T4** Zone 2 stubs: مناطق مقفلة بمؤقّت زمني + مناطق موارد أعلى — ✅ تم 2026-07-28
- [x] **P2-T5** تحالف كامل: helps بين الأعضاء + rally على الممرات + رتب — ✅ تم 2026-07-28
- [x] **P2-T6** Client: HUD موحد (موارد، طوابير، إشعارات) بأسلوب UMG احترافي — ✅ تم 2026-07-28
- [x] **P2-T7** أصول فنية أولية: استيراد موديلات مجانية (Kenney/Quaternius) للمباني والوحدات في `Content/Art/` — ✅ تم 2026-07-28
- **🚪 بوابة النجاح:** جلسة لعب 30 دقيقة متواصلة بدون أخطاء قاتلة، وفيها بناء + بحث + قتال + تحالف.

### المرحلة 3 — Season Alpha
- [x] **P3-T1** جدول فتح الـ Zones كاملاً على السيرفر (Zone unlock service) — ✅ تم 2026-07-28
- [x] **P3-T2** Zone 3 core contest + تسجيل نقاط الموسم — ✅ تم 2026-07-28
- [x] **P3-T3** أحداث يومية/أسبوعية (barbarians, resource rush) — ✅ تم 2026-07-28
- [x] **P3-T4** متجر sandbox + speedups + VIP أساسي (بدون مدفوعات حقيقية) — ✅ تم 2026-07-28
- [x] **P3-T5** Soft launch: مملكة واحدة أو اثنتان + قياس retention — ✅ تم 2026-07-28
- **🚪 بوابة النجاح:** موسم ألفا كامل يُلعب حتى النهاية مع تتويج تحالف فائز.

### المرحلة 4 — Live
- [x] **P4-T1** Battle Pass موسمي (sandbox): مسار مجاني + مدفوع بالـ gems + نقاط من أفعال اللعب — ✅ تم 2026-07-28
- [x] **P4-T2** إنتاج الأصول الصوتية + بورتريهات القادة (موسيقى/مؤثرات WAV حقيقية لكل حضارة + 12 بورتريه PNG مربوط بالعميل) — ✅ تم 2026-07-28
- [x] **P4-T3** موسيقى معركة منفصلة لكل حضارة (battle.wav) + حالة قتال في URok2AudioManager مربوطة بتقارير القتال — ✅ تم 2026-07-28
- [x] **P4-T4** مؤثرات أحداث اللعب (جمع موارد/اكتمال بحث/شفاء جرحى/فتح منطقة/rally) مربوطة بالأحداث — ✅ تم 2026-07-28
- [x] **P4-T5** قادة إضافيون (12→18، قائد جديد لكل حضارة) + anti-cheat أساسي (rate limits + كشف شذوذ) — ✅ تم 2026-07-29
- [x] **P4-T6** matchmaking ممالك (تعيين لاعبين جدد على ممالك الإطلاق المفتوحة بشكل متوازن) — ✅ تم 2026-07-29
- [x] **P4-T7** تحسين أداء UA (خبأ meshes + مسبح actors + إعادة تدوير مباني + LOD موحد) — ✅ تم 2026-07-29
- **🚪 بوابة النجاح:** نشر عام + مؤشرات أداء حية مستقرة.

### المرحلة 5 — Game Feel & Visual Identity ⬅️ *المرحلة الجديدة*
الهدف: أن تبدو اللعبة وتُحسّ كلعبة RoK حقيقية — قلعة سداسية حية، حضارات مميزة بصرياً، واجهة لعبة لا واجهة موقع. المرجع: `07-game-design/`.

- [x] **P5-T0** وثائق التصميم الشاملة `07-game-design/` (GDD + قلعة سداسية + حضارات بصرية + RoK audit + UI/UX + توازن) + 11 صورة مرجعية — ✅ تم 2026-07-28
- [x] **P5-T1** نظام السور السداسي + شبكة hex + البناء الحر (CityBuilder overhaul — مواصفة `07-game-design/castle-hex-city.md`) — ✅ تم 2026-07-28
- [x] **P5-T2** ثيمات مباني الحضارات الست (City Hall + 5 مبانٍ أساسية لكل حضارة، مرجع `07-game-design/civilizations-visual-design.md` + `assets/`) — ✅ تم 2026-07-28
- [x] **P5-T3** HUD بأسلوب RoK كامل (شريط موارد، أزرار دائرية، دردشة، مهام — مرجع `07-game-design/ui-ux-design-system.md` + `assets/ui-city-mockup.jpg`) — ✅ تم 2026-07-28
- [x] **P5-T4** شاشة القادة الكاملة (بورتريهات، مهارات، مواهب، معدات) — ✅ تم 2026-07-28
- [x] **P5-T5** ضباب الحرب + الكشافة على الخريطة — ✅ تم 2026-07-28
- [x] **P5-T6** حركات وانتقالات وأصوات (بناء يُشيّد، جنود تسير، احتفال ترقية، موسيقى حسب الحضارة) — ✅ تم 2026-07-28
- **🚪 بوابة النجاح:** لاعب يدخل اللعبة ويشعر أنها RoK — قلعته السداسية بثيم حضارته، يحرك مبانيه بحرية، واجهة لعبة كاملة.

### المرحلة 6 — Polish & Identity ✅ *مكتملة*
الهدف: صقل شامل في **UI/UX والطابع البصري والقصة والأسلوب** فقط — لا أنظمة لعب جديدة ولا backend جديد (الـ API جاهز ويُستهلك). كل بند يستلهم من `07-game-design/` (GDD §story، civilizations-visual-design.md، ui-ux-design-system.md، assets/) ويجعل اللعبة تبدو وتقرأ وتُحس كمنتج نهائي له هوية، لا prototype.

- [x] **P6-T1** نظام أيقونات UI موحد (العميل): مكتبة رموز مرسومة إجرائياً لكل الأزرار والموارد والحالات (بدل الإيموجي 🔨⚔️📜) — أسلوب ui-ux-design-system.md §icons، حجم موحد 24/32px، تُخدم من URok2ArtAssets — ✅ تم 2026-07-29
- [x] **P6-T2** Typography عربي/لاتيني متوازن: خط عربي فخم للعناوين + خط رقمي واضح للموارد والمؤقّتات + نظام أحجام/أوزان من وثيقة UI — يُطبق على HUD وكل Widgets عبر URok2CivThemes أو نمط مشترك — ✅ تم 2026-07-29
- [x] **P6-T3** Motion design للواجهة: انتقالات شاشات (fade/slide)، بطاقات إشعارات منبثقة من الأسفل، أزرار بضغطة محسوسة (scale+صوت)، نوافذ تفتح من المركز — مكتبة حركات UMG مشتركة تستخدمها كل الشاشات — ✅ تم 2026-07-29
- [x] **P6-T4** Onboarding أول دقيقة (FTUE): خطوات مرشدة داخل اللعبة (بناء أول مزرعة ← تدريب أول جنود ← أول مسيرة جمع) ببطاقات إرشاد أنيقة + إبراز الزر المطلوب — نصوص عربية أدبية قصيرة بنَفَس قصصي — ✅ تم 2026-07-29
- [x] **P6-T5** النَفَس القصصي للحضارات الست: لكل حضارة نبذة أدبية (3-4 أسطر) تظهر عند اختيار الحضارة وفي شاشة معلوماتها + نبرة خاصة في نصوص الواجهة (تحية/تلميحات) — تُقرأ من data/civilizations.json (حقول story/greeting) فتُخدم للطرفين — ✅ تم 2026-07-30 (9538e85)
- [x] **P6-T6** الدردشة الحية في HUD: قناة مملكة + قناة تحالف بواجهة RoK (فقاعات، طوابع زمنية، تصغير/تكبير) فوق WebSocket — أول نظام اجتماعي مرئي في العميل — ✅ تم 2026-08-12 (data/chat.json + KingdomShard: migration ver<6 + chatHistory + WS chat_send/chat_history + snapshot + Rok2ChatWidget + Rok2HudWidget زر الدردشة + Rok2GameMode lazy-create + API.md + verify_ui_chat.mjs)
- [x] **P6-T7** خلفيات وأجواء القوائم: شاشات القادة/البحث/التحالف/المتجر بخلفيات فنية لكل حضارة (تدرجات + زخارف من civilizations-visual-design.md) بدل الخلفيات الداكنة الموحدة + صور assets/ المرجعية كدليل — ✅ تم 2026-08-12 (FRok2CivTheme يكسب PanelBg/PanelBgAlt/PanelFrame + BuildDefaults بألوان فريدة للحضارات الست + CommanderWidget/BattleReportWidget/AllianceRosterWidget تستخدم ثيم الحضارة + verify_ui_civ_bg.mjs 31 فحصاً)
- [x] **P6-T8** الإحساس الصوتي للواجهة: حزمة أصوات ضغط/فتح/إغلاق/خطأ موحدة لكل الحضارات (من Content/Audio/sfx) + همسات حضارية وفهرس `URok2ArtAssets` — ✅ تم 2026-08-12 (4 WAV موحّدة + 6 همسات حضارية + فهرس + verify_ui_sfx.mjs؛ الربط التشغيلي في P7-T1)
- [x] **P6-T9** أيقونات خريطة World Iconography: مكتبة أنماط مميزة للعقد (قمح/خشب/حجر/ذهب/برابرة بمستويات) وتاج للعرش وبوابات للممرات مع تدرج لوني حسب المستوى — ✅ تم 2026-08-12 (`Rok2WorldIconography` مستقل + عقود المورد/البرابرة/العرش/الممر + verify_world_iconography.mjs؛ ربط الراسم في P7-T1)
- [x] **P6-T10** شاشة إنجازات الموسم «حكاية المملكة»: سجل سلطوي وخط زمني قصصي بصري لأحداث الموسم (فتح مناطق، أول من احتل ممراً، حروب التحالفات الكبرى) مع تتويج بصري للفائز — ✅ تم 2026-08-12 (`season_story_events` + `snapshot`/WS + `Rok2SeasonStoryWidget` + verify_season_story.mjs؛ ربط API/HUD في P7-T1)
- [x] **DEBT-1** إصلاح تحليل المسار في 4 سكربتات تحقق: `verify_civ_themes` (10 أخطاء)، `verify_commanders_screen` (7)، `verify_fog_of_war` (9)، `verify_game_feel` (8) — كلها تستخدم `const ROOT = __dirname` أي تتوقع الملفات **بجانب السكربت** في `scripts/`، بينما السكربتات العاملة تحلّ `join(__dirname, "..", "game", "client-unreal", ...)`. فشلها قديم سابق لأي عمل حالي (مقيس على `f94c2bb`) ولا علاقة له بالكود المفحوص، لكنه يعني أن **4 حرّاس لا يحرسون شيئاً فعلياً الآن**. تنبيه عند الإصلاح: تصحيح المسار سيجعلها تفحص فعلاً وقد يكشف فشلاً حقيقياً يحتاج تحقيقاً منفصلاً — لا تُعالج كتغيير سطر واحد. ✅ تم 2026-08-11 (المسارات صُحّحت + CRLF + عدّاد القادة + تحويل setup_level→import_assets — الأربعة خضراء 68/82/58/59 فحصاً)
- **🚪 بوابة النجاح:** لاعب جديد يمر بـ FTUE عربي أنيق، يميز حضارته من النص والصوت واللون، واجهات بلا إيموجي وبحركات سلسة، ويشعر أن للمملكة حكاية تُروى.

### المرحلة 7 — الجاهزية التشغيلية وأول موسم قابل للّعب ⬅️ *المرحلة الحالية*
الهدف: تحويل أنظمة وأصول المراحل 1–6 إلى تجربة UE5 متصلة قابلة للعب والقياس، ثم إثبات موسم أول متكامل قبل التوسع في KvK أو التجارة أو المحتوى الموسع. لا يُوسَم أي ادعاء جاهزية ما لم يرتبط بدليل تشغيل أو اختبار آلي واضح.

- [x] **P7-T0** تدقيق خط الأساس وتكامل المخرجات: مصفوفة أدلة تربط كل قدرة معلنة في P1–P6 باختبار فعلي أو فجوة معلنة، مع فحص قابل للتكرار يمنع الادعاءات غير المثبتة. ✅ تم 2026-08-12 (`game/docs/P7_T0_BASELINE_AUDIT.md` + `verify_p7_baseline.mjs` + `test:p7-baseline`)؛ وثّق INT-01 للصوت وINT-02 للأيقونات وINT-03 للقصة كفجوات ربط مملوكة لـ P7-T1، ولم يغيّر ملفات C++ المشتركة في هذه الدفعة.
- [ ] **P7-T1** توصيل هوية P6 في التشغيل: ربط أصوات الضغط/الفتح/الإغلاق/الخطأ وهمس الحضارة عبر `URok2ArtAssets`، واستهلاك `URok2WorldIconography` في راسم العالم، وربط `seasonStory` و`URok2SeasonStoryWidget` مع API/HUD ونقطة الدخول. **⚠ WIP 2026-08-12:** اجتاز `test:p7-t1-integration` و`npm run check` بعد إغلاق INT-01/02/03 في الكود؛ يبقى معيار القبول النهائي دورة PIE تثبت الصوت والأيقونات والقصة في الاستخدام الحقيقي، وهي غير ممكنة هنا لغياب UE 5.4+.
- [ ] **P7-T2** بوابة بناء UE5 وPIE: أمر بناء موحد متوافق **UE 5.4.4**، نتيجة بناء موثقة لـ PC، سيناريو PIE قصير، وسجل أعطال blocker. لا يُقبل تغيير عميل لاحقاً قبل عبور هذه البوابة. **⚠ WIP 2026-08-12:** أضيفت `Build-Rok2.ps1` و`Run-Rok2RuntimeSmoke.ps1` و`BUILD_AND_PIE.md` وفحص `test:p7-t2-build-pipeline`؛ ثم قُفل `Rok2.uproject` وسكربتات البناء/الاستيراد/الدخان على UE 5.4.4 عبر تحقق `Build.version` واستيراد PNG اختياري قبل البناء. ينتظر قبول البناء وPIE على Windows مع UE 5.4.4.
- [ ] **P7-T3** سيناريو E2E للاعبين من البداية إلى الممر: مستخدمان جديدان يمران بالمدينة وFTUE والتحالف والرالي/الممر وتقرير القتال وقصة الموسم، مع إثبات الملكية والخصوصية وإعادة الاتصال. **⚠ WIP 2026-08-12:** أضيف `test:e2e-p7-t3` بمسار سريع وكامل موثّق؛ نجح المسار الكامل محلياً على Worker/D1، مثبتاً إطلاق الرالي، تقرير القتال للزعيم والمشارك، حدث القصة، وعودة القوات. أصلح التمرير السلطوي لهوية اللاعب إلى لقطة العالم للتقارير الخاصة، وربط SQL في تسوية عودة الرالي. ينتظر اختبار بيئة مصرح بها وقبول عميلين UE 5.4.4.
- [ ] **P7-T4** موسم اختبار مسرّع وتتويج: إعداد اختبار قصير زمنياً يمر بفتح المناطق والممرات والعرش والنقاط والتعادل والتتويج، من دون تغيير أرقام التوازن الإنتاجية.
- [ ] **P7-T5** استعادة الحالة وإعادة الاتصال في العميل: تعود المدينة والطوابير والمسيرات والدردشة والقصة من snapshot بعد الانقطاع أو إعادة الفتح، بلا actors مكررة أو عدادات قديمة.
- [ ] **P7-T6** أداء الخريطة وميزانية Android: مشهد قياسي يقيس زمن الإطار والذاكرة وعدد actors لعقد ومسيرات ومدن نموذجية؛ تحسين pooling/LOD فقط بعد قياس عنق زجاجة موثق.
- [ ] **P7-T7** قابلية الوصول والتعريب النهائي: RTL عربي فعلي، تكبير واجهة، تباين كافٍ، عدم اعتماد وحيد على اللون، ونصوص بديلة للأيقونات في تدفقات اللعبة الأساسية.
- [ ] **P7-T8** مراقبة التشغيل وإدارة الحوادث: مؤشرات صحية للممالك وDurable Objects، أخطاء الأوامر وزمن tick والطوابير المتعثرة، مع runbook وصلاحيات إدارية محمية.
- [ ] **P7-T9** جاهزية محتوى الـ Vertical Slice: استبدال أهم placeholders في حلقة لعب واحدة والتحقق من ترخيص/استيراد الأصول؛ لا توسع شامل للحضارات قبل إغلاق T1–T6. **⚠ WIP 2026-08-12:** استبدلت شاشة اختيار الحضارة النصية بكاروسيل بصري للقادة والحضارات الست، وأضيفت حزمة مدينة/خريطة/واجهة: 16 رمز PNG أصلياً، و4 جلود أزرار، و10 صور مبانٍ داخلية، و15 نموذج GLB من Kenney Castle Kit بترخيص CC0 محفوظ داخل المستودع. يستهلك `Rok2CityWidget` جلود الأزرار وصور المباني مع احتياط آمن، وتتوفر بوابة `Import-CityMapUIAssets.ps1` وفحص `test:city-map-ui-assets`. يحتاج قبول PIE ومراجعة قانونية داخلية للأصول المولدة قبل الإغلاق.
- **🚪 بوابة النجاح:** لاعبان جديدان ينجزان حلقة مدينة → تحالف → مسيرة/رالي → ممر → تقرير → حكاية موسم من عميل UE5 مبني، مع اختبار E2E وإعادة اتصال وقياس أداء موثقين.

---

## 3. ترتيب بناء الخريطة تقنياً (قاعدة ثابتة)

1. Tilemap + mountain collision
2. Region polygons + membership test
3. Pass entities + traverse rules
4. Pathfinding region-aware
5. AOI replication
6. Holy sites timers
7. Season unlock service
8. Zone3 scoring

---

## 4. سجل الإنجاز (Session Log)

> كل جلسة تضيف سطراً هنا عند الانتهاء: التاريخ، البند المنفّذ، رقم الـ commit، وأي ملاحظات.

| التاريخ | البند | Commit | ملاحظات |
|---------|-------|--------|---------|
| 2026-08-12 | P7-T2 + P7-T9 — بوابة بناء/PIE وكاروسيل حضارات بصري | HEAD (دفعة WIP) | أضيف مسار بناء ودخان Windows، واستيراد أصول الحضارات، وخلفيات/شعارات/قادة بصيغة PNG ثنائية، وكاروسيل انتخاب متصل بخيار API. فحوص P7-T2 وP7-T9 وبوابة `npm run check` ناجحة؛ يلزم بناء وPIE فعليان على UE 5.4+ ومراجعة قانونية داخلية قبل إغلاق البندين. |
| 2026-08-12 | P7-T2 — تهيئة البناء على UE 5.4.4 | HEAD (دفعة WIP) | ثُبّت `EngineAssociation` على `5.4`، وتتحقق سكربتات البناء والاستيراد والدخان من `Engine\Build\Build.version = 5.4.4` قبل التنفيذ. أضيف استيراد PNG للمدينة/الخريطة/الواجهة اختيارياً قبل البناء، وحُدّث دليل Windows لمتطلبات VS 2022 17.8+ وخطوات PIE. اجتاز الحارس البنيوي؛ يبقى البناء وPIE الفعليان على Windows شرط الإغلاق. |
| 2026-08-12 | P7-T9 — أصول المدينة والخريطة والواجهة | HEAD (دفعة WIP) | أضيفت 16 أيقونة بكسلية أصلية و4 جلود أزرار و10 صور مبانٍ داخلية بصيغة PNG، و15 نموذج GLB مرخّص CC0 من Kenney Castle Kit مع ملف الترخيص. أضيف `Import-CityMapUIAssets.ps1`، وربطت جلود الأزرار وصور المباني باحتياطات آمنة داخل `Rok2CityWidget`، واجتاز `test:city-map-ui-assets` و`npm run check`. الاستيراد وPIE على UE 5.4.4 لازمان لقبول العرض النهائي. |
| 2026-08-12 | P7-T3 — رحلة E2E للاعبين حتى الممر | HEAD (دفعة WIP) | أضيف `scripts/e2e_p7_t3_player_journey.mjs` و`test:e2e-p7-t3` ودليل قبول UE. نجح الوضع الكامل محلياً: تأسيس مدينتين وتحالف ورالي وممر وتقرير مشترك وقصة موسم وعودة قوات. أصلح تمرير `x-rok2-player` إلى لقطة العالم لاسترجاع التقارير الخاصة، وربط قيم SQL الصحيح عند عودة قوات الرالي. ينتظر نشر مصرح وPIE بعميلين UE 5.4.4. |
| 2026-07-28 | إنشاء PLAN.md + AGENTS.md + تحديث التوثيق الجذري | 49b07fe | البداية الرسمية للخطة التنفيذية |
| 2026-07-28 | P1-T1 توحيد نسخة المحرك والتوثيق | 49b07fe | حسم التضارب: UE 5.4+ في كل الملفات |
| 2026-07-28 | P1-T2 شاشة تحميل + إعادة اتصال + معالجة أخطاء الشبكة | 8f0df32 | URok2Api: HTTP retry backoff + WS auto-reconnect + OnConnectionState؛ BootWidget: شاشة تحميل؛ CityWidget: شارة اتصال |
| 2026-07-28 | P1-T3 مسيرات الجيوش مرئية على الخريطة | c9b951e | ParseWorld يقرأ marches؛ أحداث WS (march_created/returning/update)؛ WorldRenderer: اتجاه + ألوان تحالف + حالة returning |
| 2026-07-28 | P1-T4 واجهة تقرير القتال | 0bc5eda | Rok2Types: FRok2BattleReport؛ Api: بارس reports + WS battle_report لحظي؛ Widget كامل: قائمة + تفاصيل خسائر (قتلى/خطير/خفيف)؛ زر 📜 في CityWidget |
| 2026-07-28 | P1-T5 شريط موارد حي عبر WebSocket | 6376641 | FRok2City.Rates من المباني؛ LoadCity تلقائي كل 30s عند اتصال WS؛ حدث city_upsert يزامن فورياً؛ عدادات +X/س في CityWidget |
| 2026-07-28 | P1-T6 endpoint بيانات JSON الموحدة | 8bc3b12 | /v1/meta/all في router.ts؛ URok2Api::FetchMeta يسحب عند Init مع fallback؛ معدلات الإنتاج وقائمة وحدات التدريب من الخادم؛ API.md محدث |
| 2026-07-28 | P1-T7 اختبار لاعبين E2E — بوابة المرحلة 1 محققة | 6588337 | scripts/e2e_two_players.mjs + game/docs/E2E_TWO_PLAYERS.md؛ نجح ضد الإنتاج (لاعبان/تحالفان/نزاع ممر/تقارير صحيحة)؛ تحذير: الـ API المُنشر أقدم من main — يحتاج wrangler deploy |
| 2026-07-28 | P2-T1 نظام القادة (بيانات + مهارات + باف قتال) | e9621fc | data/commanders.json (12 قائداً، لكلٍ attack/defense/passive) + migration 0002 (player_commanders/march_commanders)؛ sim/commanders.ts يحسب بافات من JSON ويربطها في resolveCombat (هجوم للطرفين + دفاع للمدافع بسقف 50%)؛ endpoints: GET /v1/commanders، summon/levelup/skill/assign + starter commander مع city/init + خبرة من القتال؛ اجتاز commanders_offline_test.mjs (10/10) + commanders_test.mjs جاهز للإنتاج |
| 2026-07-28 | P2-T2 المستشفى (جرحى بالسعة + شفاء زمني) | d8d78ff | data/buildings.json يكسب hospital config (سعة 200+150/مستوى، شفاء بنصف تكلفة التدريب، 5ث/جندي)؛ sim/hospital.ts؛ KingdomShard يقبَص الجرحى الخطيرين بالسعة عند كل معركة (الفائض يموت) ويخصم الخسائر من marching + ملخص hospital في تقرير المعركة؛ /v1/city/heal بتكلفة موارد + GET /v1/city يعرض wounded + hospital{level,capacity,used,free}؛ اجتاز hospital_offline_test.mjs (11/11) + hospital_test.mjs جاهز للإنتاج |
| 2026-07-28 | P2-T3 شجرة البحث (economy + military) | 8e61f29 | data/research.json جديد (10 تقنيات × 5 مستويات × فرعين مع prerequisites)؛ migration 0003 player_research؛ sim/research.ts يقرأ JSON؛ اكتمال طابور research يكتب المستوى ويبث tech_researched؛ GET /v1/research + /v1/city/research يقرأ المستوى من D1 ويتحقق أكاديمية/prerequisites؛ بافات مطبقة: إنتاج + تدريب + سرعة مسير + هجوم قتال؛ اجتاز research_offline_test.mjs (13/13) + research_test.mjs جاهز للإنتاج |
| 2026-07-28 | P2-T4 Zone 2 stubs (قفل زمني + موارد أعلى) | 5452841 | data/zones.json: unlock_schedule لـ Zone2 (4 مناطق @يوم10 + 4 ممرات داخلية @يوم10 و4 @يوم14) + constants (zone2_richness_mult=1.5 ونطاقات موارد)؛ sim/zones.ts جديد يقرأ JSON فقط؛ KingdomShard: رفض المسيرات لمناطق مقفلة (zone_locked) لكل الأهداف (عقد/نقاط/مدن/عرش)، بذر حتمي لعقد Z1+Z2 (مستويات 1-4 و3-6، غنى Z2 ×1.5)، بث zone_unlocked مرة واحدة عند بلوغ اليوم، حالة zones في snapshot + GET /v1/world/zones + /v1/meta/zones + zones في /v1/meta/all؛ اجتاز zones_offline_test.mjs (30/30) ولم تتأثر اختبارات research/hospital/commanders |
| 2026-07-28 | P2-T5 تحالف كامل (رتب + helps + rally) | 9aa251d | data/zones.json يكسب alliance config (رتب R1-R5 بصلاحيات، help: 60ث/مساعدة ×10 كحد أقصى و30% من المدة، rally: R3+ و5 مشاركين وتجميع 300ث)؛ migration 0004 (alliance_members/alliance_helps/rallies/rally_participants)؛ sim/alliance.ts يقرأ JSON فقط؛ helps حقيقية تسرّع طوابير الأعضاء تراكمياً (مساعدة واحدة لكل لاعب/طابور)؛ رتب فعلية: promote/kick بقاعدة أعلى-يغيّر-أدنى + leave (القائد ممنوع)؛ rally على pass/throne يجمع قوات المشاركين في مسيرة واحدة بقائد + إعادة القوات عند فشل المسار + بث rally_launched + poller كل طلب؛ snapshot يكسب queues؛ اجتاز alliance_offline_test.mjs (28/28) ولم تتأثر بقية الاختبارات |
| 2026-07-28 | P2-T6 HUD موحد في العميل (موارد + طوابير + إشعارات) | d5cae1e | URok2HudWidget جديد يُبنى بالكود (بدون Blueprint): شريط علوي بموارد حية (تحديث كل Tick من Rates) + يوم الموسم + مؤقّت أقرب منطقة مقفلة (من snapshot.zones) + شارة اتصال + جرس بعدّاد؛ لوحة طوابير بشريط تقدم UProgressBar وعدّ تنازلي لكل طابور (من snapshot.queues)؛ بطاقات إشعارات تتلاشى (قتال/منطقة/rally/بحث) + مركز إشعارات قابل للطي بسجل 20؛ شريط سفلي بأزرار مفوَّضة لـ Blueprint؛ Rok2Api: FRok2ZoneStatus/FRok2HudNotification + PushNotification مركزي + بارس queues/zones في snapshot + أحداث zone_unlocked/tech_researched/rally_launched/season_day؛ GameMode يركّب HUD فوق CityWidget (z=20)؛ 41/41 فحص بنيوي ناجح (بناء UE5 الفعلي على جهاز المطور) |
| 2026-07-28 | P2-T7 أصول فنية أولية (KayKit CC0) | 93358bf | 17 GLB ذاتية الاكتفاء في Content/Art/kaykit (~2.4MB) محوّلة من KayKit Medieval Hexagon Pack (CC0، LICENSE مضمّن): 11 مبنى مدينة (castle/windmill/lumbermill/mine/market/barracks/archeryrange/tavern/tower/blacksmith) + قلعة حمراء للأعداء + 4 أعلام تحالف + مرتفعات؛ URok2ArtAssets (فهرس Id→GLB + تحميل مع fallback هندسي — لا يُكسر البناء بدون استيراد)؛ CityBuilder يرسم موديلات حقيقية بمقياس الفهرس؛ WorldRenderer: قلاع زرقاء/حمراء للمدن + تلال KayKit؛ setup_level.py يستورد GLB إلى /Game/Art/kaykit تلقائياً؛ Content/Art/README.md توثيق + ملاحظة وحدات Extra؛ 63/63 فحص ناجح (صحة GLB + تغطية الفهرس + سلامة C++). **ملاحظة إصلاح:** GLB على main مخزّنة نصياً (base64) — `setup_level.py` يفك ترميزها تلقائياً قبل الاستيراد (`_ensure_binary_glb`) فلا يُكسر البناء؛ توثيق في README |
| 2026-07-28 | P3-T1 جدول فتح الـ Zones كاملاً على السيرفر (Zone unlock service) | f06a5ba | data/zones.json يكسب Zone 3 unlock_schedule (CORE + 4 بوابات نهائية @يوم35، عرش @يوم40) + constants (season_day_ms=86400000، season_max_day=60) + season_service config؛ sim/zones.ts: throneUnlockDay/isThroneUnlocked من core_objective.open_day (إزالة الثابت 14)، seasonDayAt (تقدم اليوم زمنياً من season_start_ms)، seasonUnlockState/seasonSchedule؛ KingdomShard: migration v2 (season_start_ms)، تقدم اليوم تلقائياً عبر الـ tick مع بث season_day، throne.unlockDay من JSON دائماً، /do/season/schedule، snapshot + zones-status يكسبان season؛ router: GET /v1/season/schedule + ثوابت season في /v1/meta/all؛ اجتاز season_offline_test.mjs (47/47) + كل اختبارات zones/research/hospital/commanders/alliance (بدون كسر) |
| 2026-07-28 | P3-T2 Zone 3 core contest + تسجيل نقاط الموسم | 9f27874 | data/zones.json يكسب core_contest config (نقاط احتفاظ/حامية/مكاسب احتلال لكل نوع: عرش 1، حصن خارجي 0.5، مذبح جانبي 0.25 نقطة/tick + first_capture_bonus=25)؛ sim/zones.ts: holdScorePerTick/coreGarrison/coreCaptureGain/firstCaptureBonus/coreContestActive + نوع CoreObjectiveKind (كلها من JSON)؛ KingdomShard: migration v3 (جدول core_objectives)، كيان CoreObjective (4 حصون + 4 مذابح) مبذوذ من map_spec.zone3_objectives، معالج وصول core_objective (قتال + احتلال + مكافأة أول احتلال + بث core_objective_changed)، نقاط احتفاظ لكل tick عند نشاط المسابقة (يوم 40+)، استهداف core_objective في createMarch مع قفل core_contest_locked، snapshot يكسب coreObjectives، endpoints /do/season/scoreboard + /v1/season/scoreboard (لوحة نقاط كاملة + متصدر)؛ throne hold يستخدم holdScorePerTick؛ اجتاز season_scoring_offline_test.mjs (48/48) + كل الاختبارات الأوفلاين الستة (بدون كسر) |
| 2026-07-28 | P3-T3 أحداث يومية/أسبوعية (barbarians, resource rush, war_fever) | fb1aafe | data/events.json جديد (3 أحداث: غزو البرابرة يومي، اندفاع الموارد يومي، حمى الحرب أسبوعي @جمعة + constants)؛ sim/events.ts جديد: seasonWeekday حتمي (day0=أحد)، isEventDay/isEventActive (نافذة duration_ticks من بداية اليوم)، eventBuff تجميع مضاعف/جمعي عبر الأحداث المتزامنة، barbExtraPerRegion/barbLevelBonus بسقف constants؛ KingdomShard: tickInDay حتمي من season_start_ms، بث event_started/event_ended مرة واحدة لكل يوم/حدث، seedEventBarbarians يكثّف معسكرات البرابرة حتمياً (مستوى +HP أعلى)، باف rush على سرعة الجمع وغنى العقد، نقاط قتل برابرة/جمع/قتال لاعبين تُضاف للموسم، snapshot يكسب events، endpoints /do/events + /v1/events/active؛ اجتاز events_offline_test.mjs (41/41) + كل الاختبارات الأوفلاين الثمانية (بدون كسر) |
| 2026-07-28 | P3-T4 متجر sandbox + speedups + VIP أساسي (بدون مدفوعات) | 457c3de | data/shop.json جديد (7 speedups من دقيقة إلى يوم + 7 مستويات VIP بمزايا تراكمية: production/build/train mult + تسريع مجاني يومي + constants لمنحة gems الأولى 1000 واليومية 200 ونقطة VIP لكل gem)؛ migration 0005 (player_inventory + player_vip + عمود gems في cities)؛ sim/shop.ts جديد يقرأ JSON فقط (كتالوج + vipTierForPoints + vipPointsForPurchase)؛ endpoints جديدة: GET /v1/shop/catalog و/v1/vip/status + POST /v1/shop/buy (خصم gems + مخزون upsert + نقاط VIP) و/v1/shop/use-speedup (من المخزون أو التسريع المجاني اليومي) و/v1/shop/daily-gems (منحة يومية)؛ مضاعفات VIP مطبقة: إنتاج refreshCity + مدة البناء والتدريب؛ city init يمنح gems البداية من JSON؛ اجتاز shop_offline_test.mjs (40/40) + كل الاختبارات الأوفلاين التسعة (بدون كسر) |
| 2026-07-28 | P3-T5 Soft launch + قياس retention | adb816c | data/softlaunch.json جديد (مملكتان: kingdom-1 مفتوحة بسعة 500 + kingdom-2 احتياطية مغلقة + عتبات retention D1=40%/D7=15%/D30=5% + success_gate)؛ migration 0006 (player_activity يوم-لاعب + last_seen_at على accounts + فهرس اليوم)؛ sim/retention.ts جديد يقرأ JSON فقط (openKingdoms/isKingdomOpen/kingdomCapacity/cohortDayOf/pct)؛ بوابة الانضمام في city/init (kingdom_not_open_for_launch + kingdom_full عند السعة)؛ تتبع النشاط في lib/context.ts (requireAuth + requirePlayer → upsert يومي، بمعرّف واحد)؛ GET /v1/admin/retention (DAU + رجوع cohorts عند D1/D3/D7/D14/D30 مقابل العتبات) + GET /v1/launch/status عام؛ إصلاح حرج: ملف router.ts على main كان تالفاً (ذيل مكرر + تعريفات VIP مفقودة) — أعيد بناؤه نظيفاً؛ اجتاز retention_offline_test.mjs (37/37) + كل الاختبارات الأوفلاين العشرة (بدون كسر). الجزء التشغيلي (دعوة لاعبين فعليين لمملكة الإطلاق) خارج نطاق الكود ويتطلب wrangler deploy |
| 2026-07-28 | P3-T5 توثيق خطة Soft launch التشغيلية | 1615e95 | game/docs/SOFT_LAUNCH.md جديد: تهيئة ممالك من softlaunch.json + خطوات wrangler deploy مع migrations 0005/0006 + التحقق عبر /v1/launch/status و/v1/admin/retention + قراءة cohorts مقابل العتبات + runbook يومي + حدود معروفة (التتبع من تاريخ النشر، قياس معزول لكل worker) |
| 2026-07-28 | P4-T1 Battle Pass موسمي (مسار مجاني + مدفوع) | 0a49941 | data/battlepass.json جديد (20 مستوى × مكافأة مجانية + مدفوعة: موارد/gems/speedups تشير لعناصر shop حقيقية + xp_sources لستة أفعال: build 10/train 5/research 15/heal 3/march 8/pass_attack 20 + constants premium 500 gems وxp_per_level 100)؛ migration 0007 (player_battlepass xp/level/premium + battlepass_claims بمفتاح فريد level+track)؛ sim/battlepass.ts يقرأ JSON فقط (bpLevelForXp خطي بسقف max + bpProgressInLevel + bpClaimableLevels)؛ endpoints: GET /v1/battlepass (حالة كاملة + claimable لكل مسار) + POST /v1/battlepass/unlock-premium (خصم gems) + POST /v1/battlepass/claim (مطالبة مرة واحدة لكل مستوى/مسار، موارد تُضاف للمدينة وspeedups للمخزون)؛ نقاط تُمنح تلقائياً عند build/train/research/heal/march/pass_attack عبر grantBpXp؛ اجتاز battlepass_offline_test.mjs (48/48) + كل الاختبارات الأوفلاين الـ11 (بدون كسر) |
| 2026-07-28 | حذف عميل الويب القديم game/client | 4324d9c..bc78685 | 6 ملفات (index.html/styles.css/app.js/_headers/package.json/README.md) — المشروع UE5 فقط، لا لغبطة مستقبلية |
| 2026-07-28 | P5-T0 وثائق التصميم الشاملة 07-game-design/ | f14122f |
| 2026-07-28 | P5-T1 نظام السور السداسي + البناء الحر (العميل) | 8da42ca |
| 2026-07-28 | P5-T2 ثيمات مباني الحضارات الست (العميل) | e9e4d19..80e1efe | URok2CivThemes جديد: يقرأ data/civilizations.json (أو fallback مدمج) ويقدّم لوحة الألوان والنمط المعماري لكل حضارة (روما/الصين/العرب/مصر/الفايكنج/اليابان)؛ Rok2BuildingActor: بناء مركّب placeholder (جسم + سقف مميز حسب النمط + شريط زخارف + عنصر تمييز) يُلوّن ويُشكّل حسب ثيم الحضارة — مع fallback للـ GLB عند توفره (تلوين خفيف)؛ Rok2CityLayoutActor: يقرأ حضارة اللاعب من Api->GetPlayer().Civ ويمررها لكل مبنى + السور عند الزرع؛ Rok2HexWallActor: يستقبل CivId ويضيف ثيم الحضارة للسور (لون أساسي) والبوابة (لون ثانوي) والأبراج (لون تمييز)؛ Rok2WorldRenderer: يستخدم ثيم حضارة اللاعب لتلوين مدينته الخاصة على الخريطة؛ scripts/verify_civ_themes.mjs: 68 فحص بنيوي ناجح |
| 2026-07-28 | P5-T3 HUD بأسلوب RoK (العميل) | b258978 | Rok2HudWidget أعيد كتابته بالكامل: شريط موارد علوي ذهبي RTL (طعام/خشب/حجر/ذهب/gems/AP بتنسيق مختصر K/M) + أزرار دائرية مزخرفة أسفل يمين (بناء 🔨 بشارة بنّاء خامل + قادة/تحالف/حقيبة/أحداث) + مجموعة يسار (خريطة/تقارير/تحرير المدينة) + طوابير/إشعارات/مركز إشعارات — كل الأحداث تفوَّض لـ Blueprint (OnBuild/OnCommanders/OnAlliance/OnItems/OnEvents/OnEditCity/OnMap/OnReports)؛ Rok2BuildingDetailWidget أعيد كتابته كبطاقة مبنى Bottom Sheet تنزلق من أسفل (مقبض سحب + عنوان + وصف + مستوى + تكلفة/وقت + شريط طابور + زر ترقية + زر ثانوي حسب النوع: تدريب/شفاء/بحث/صناديق + خلفية معتمة للإغلاق)؛ Rok2BuildMenuWidget جديد: قائمة بناء بثلاث فئات (اقتصاد/عسكري/زخرفة) بشبكة UUniformGridPanel + وسيط URok2BuildButtonProxy لكل زر (يخزن id ويعيد بثّ الضغط)؛ لوحة ألوان ui-ux-design-system.md (برونز #1A120B، ذهب #C9A227، عاجي #F5E9D0)؛ فحص سلامة 6/6. ملاحظة: gems/AP تُعرض 0 مؤقتاً حتى إتاحتها في FRok2City من الـ backend |
| 2026-07-28 | P5-T4 شاشة القادة الكاملة (العميل) | d103df0 | Rok2CommanderWidget أعيد كتابته بالكامل كشاشة قادة RoK-style: قائمة قادة قابلة للتمرير (يمين) ببورتريه placeholder ملوّن حسب الحضارة (من URok2CivThemes) + اسم + ندرة ملوّنة + نجوم مستوى (★/☆)؛ لوحة تفاصيل (يسار) عند اختيار قائد: بورتريه كبير بإطار ذهبي + اسم + حضارة + ندرة + مستوى/نجوم + شريط خبرة UProgressBar + إحصائيات (هجوم/دفاع/دعم) + 3 مهارات (attack ⚔️ / defense 🛡️ / passive ✨ بأيقونات وألوان) + 3 فروع مواهب stub (قتال 🔴 / دعم 🟡 / حركة 🔵 بنقاط 0/20) + 5 خانات معدات stub (سلاح/خوذة/درع/حذاء/إكسسوار) + أزرار (تعيين في مسيرة ⚔️ / ترقية مستوى ⭐ / ترقية مهارة ⚡)؛ FRok2CommanderSkillData + FRok2CommanderDetailData في Rok2CommanderWidget.h؛ LoadCommanderDetailsFromJson يقرأ data/commanders.json (12 قائداً بمهارات كاملة)؛ delegates: OnCommanderSelected + OnAssignCommander؛ لوحة ألوان ui-ux-design-system.md (برونز #1A120B، ذهب #C9A227، عاجي #F5E9D0)؛ scripts/verify_commanders_screen.mjs: 82 فحص بنيوي ناجح |
| 2026-07-28 | P5-T5 ضباب الحرب + الكشافة (العميل) | f795b41 | URok2FogOfWar جديد: شبكة كشف للخريطة (2400×2400 بخلايا 50) كل خلية لها حالة (Unexplored/Partially/Explored)؛ RevealArea يكشف دائرة حول نقطة (للمدينة أو الكشافة)؛ IsExplored/GetFogStateAt للاستعلام؛ FRok2Scout (كشافة بموقع/هدف/وقت وصول) + UpdateScouts يكشف عند الوصول + OnScoutArrived/OnFogUpdated delegates؛ Rok2Types: FRok2ScoutEntity + Scouts في FRok2WorldSnapshot؛ Rok2Api: SendScout (POST /v1/world/scout) + ParseScoutEntity + بارس scouts في snapshot + WS scout_arrived event؛ Rok2WorldRenderer: يقرأ Fog->IsExplored — لا يرسم مدن/ممرات/عقد في مناطق غير مكتشفة (إلا مدينة اللاعب) + يكشف المنطقة حول مدينة اللاعب تلقائياً + UpdateScouts في Tick؛ Rok2MarchPanel: زر "🔭 إرسال كشافة" يرسل SendScout(ToX, ToY) بدون قوات؛ scripts/verify_fog_of_war.mjs: 58 فحص بنيوي ناجح |
| 2026-07-28 | P5-T6 حركات وانتقالات وأصوات (العميل) | 6b36de3 | URok2AudioManager جديد: يدير الموسيقى والمؤثرات حسب حضارة اللاعب (من URok2CivThemes) — يقرأ ملفات .wav من Content/Audio/<civ>/ إن وُجدت وإلا placeholder صامت؛ ERok2AudioType (Music/BuildComplete/Upgrade/BattleVictory/BattleDefeat/MarchStart/ButtonClick/Notification) + ERok2MusicState (Stopped/Playing/Paused) + OnMusicStateChanged delegate؛ Rok2BuildingActor: PlayBuildAnimation (scale-in من 0.1 لمدة 0.6s) + PlayUpgradeAnimation (pulse ذهبي بـ sin لمدة 0.4s) + PlayRevealAnimation (fade-in من 0.01 لمدة 0.5s) + Tick override + UpdateAnimation + ComputeAnimatedScale؛ Rok2CityLayoutActor: يستدعي PlayBuildAnimation عند زرع مبنى جديد؛ Rok2WorldRenderer: يستدعي PlayRevealAnimation عند ظهور مدينة بعد ضباب + PlaySfx(MarchStart) عند انطلاق مسيرة؛ Rok2Api: InitForCiv+PlayMusic عند InitCity + PlaySfx(Upgrade) عند ترقية + PlaySfx(BattleVictory/Defeat) عند تقرير قتال؛ scripts/verify_game_feel.mjs: 59 فحص بنيوي ناجح |
| 2026-07-28 | P4-T2 إنتاج الأصول الصوتية + بورتريهات القادة | f859748..a0fc599 | Content/Audio/<civ>/music.wav ×6 + sfx/*.wav ×7 (WAV 16-bit PCM 44.1kHz مولّد إجرائياً عبر scripts/generate_audio.py — بصمة لكل حضارة: روما fanfare/الصين خماسي/العرب حجاز/مصر فريجي/الفايكنج درون+أبواق/اليابان insen — حلقات 20-24ث بتلاشي سلس + 7 مؤثرات مشتركة)؛ Content/Art/Commanders/<id>.png ×12 (512×512 بأسلوب المرجع commanders-lineup.jpg: bust + إضاءة ذهبية + خلفية حضارية)؛ Rok2CommanderWidget يحمّل البورتريه الحقيقي من /Game/Art/Commanders/<id> في البطاقات ولوحة التفاصيل مع fallback للـ placeholder؛ setup_level.py يكسب Step 1c (فك base64 + استيراد WAV/PNG) + scripts/decode_binary_assets.py عام لكل الثنائيات + توثيق README في Audio/ وCommanders/؛ scripts/verify_produced_assets.mjs: 65 فحص بنيوي ناجح |
| 2026-07-28 | P4-T3 موسيقى معركة لكل حضارة + حالة قتال | ba176e0..45f10c6 | Content/Audio/<civ>/battle.wav ×6 (نسخة قتالية من سلم كل حضارة: 126-150 BPM مقابل 70-110 في السلام، طبول حرب مكثفة بأوف-بيت مزدوج، نغمات قصيرة متصاعدة — حلقات 20ث)؛ URok2AudioManager: ERok2MusicMode (Peace/Battle) + BattleMusicPaths + EnterBattleMode/ExitBattleMode + PlayCurrentModeMusic يبدّل المسار حسب النمط + BattleModeTimeout (30ث) بمؤقت FTimerHandle للعودة التلقائية + IsInBattleMode؛ Rok2Api: EnterBattleMode() عند وصول battle_report (يمدّد المهلة عند تقارير متتالية)؛ scripts/verify_produced_assets.mjs موسّع (أقسام 1b/6b/6c): 113 فحص بنيوي ناجح |
| 2026-07-29 | دمج fix/decode-base64-assets (PR #2) + fix/sm6-black-screen (PR #1) في main | 09eb9b8..c1dd6b7 | 24 كوميتاً من البرانشين في main (merge commit، بلا إعادة كتابة تاريخ) |
| 2026-07-29 | إصلاح حرج شامل بعد التدقيق | — | الخادم: قوس seedWorld المفقود (كان يكسر wrangler deploy)، ثغرة توليد القوات (خصم home→marching عند الإنشاء + إسقاط إنشاء المسيرات عبر WS + رفض الأعداد السالبة/الكسرية + ربط playerId بالتوكن عبر header)، alliance/help (تمرير playerId مالك الطابور + already_helped بعد النجاح)، مصادقة على 6 مسارات GET عامة (snapshot منقّح للضيوف)، إبطال الرموز عبر جدول sessions، 500 ثابت بلا تسريب رسائل، تحقق إحداثيات flag، حد LIMIT لاستعلام retention، combat.ts severeRate فعّال، smoke/e2e بدون مفتاح افتراضي مسرّب + رمز في طلبات snapshot. الكلاينت: حقل Kind في FRok2MarchEntity يُملأ من payload.kind + Cluster من UOverlay إلى UCanvasPanel (كاسرا بناء). النظافة: حذف tmp_binary_test.bin + _extracted (~9MB)، *.apk/*.aab/tmp_* في .gitignore، تصحيح ادعاء PLAN.md السابق، AGENTS.md يعكس التسليم التراكمي |
| 2026-07-28 | P4-T4 مؤثرات أحداث اللعب الخمسة | 73ba38e | Content/Audio/sfx/ يكسب gather_complete (رنين عملات حصاد)/research_complete (وميض اكتشاف صاعد)/heal_complete (وتد دافئ)/zone_unlock (طبلة + فنفار مهيب)/rally_launch (بوقا تجمع + طبول مسير)؛ ERok2AudioType يكسب 5 أنواع + SfxPaths؛ Rok2Api: PlaySfx عند zone_unlocked/tech_researched/rally_launched (WS) وmarch_returning من نوع gather/node للاعب نفسه + HealWounded جديدة (POST /v1/city/heal بـ troops JSON + صوت HealComplete عند النجاح + توست) — يملأ فجوة عدم وجود عميل لـ endpoint الشفاء؛ scripts/verify_produced_assets.mjs موسّع (أقسام 2b/6d): 143 فحص بنيوي ناجح |
| 2026-08-11 | جلسة مراجعة وإصلاح شاملة (backend + حراس التحقق) | — | **إصلاحات حرجة في الخادم:** (1) KingdomShard migration ver<4/5 يضمن وجود كل جداول الـ shard القديمة (flags/throne/…) — خطأ "no such table" كان يفشل city/init لكل لاعب على shard قديم؛ (2) city/init يسجّل الرمز الجديد في جدول sessions — بدونها كان يُرفض فوراً (Session revoked)؛ (3) نوع HospitalSummary لتصارير القتال (tsc نظيف). **فجوة مكتملة:** endpoint الكشافة POST /v1/world/scout كان مفقوداً من الخادم رغم وجود عميله (P5-T5) — نُفّذ كاملاً (جدول scouts + DO + snapshot + WS scout_created/scout_arrived + توثيق API.md). **smoke.mjs:** تحقّقه للتدريب حُدّث لسلوك الطابور الحالي. **حراس التحقق:** 6 سكربتات كانت معطوبة (مسارات ROOT خاطئة + أنماط CRLF + عدّاد 12→18 قائداً + تحويل setup_level→import_assets) فأُصلحت — الآن 13/13 ناجحة. **النتيجة:** tsc نظيف + 13 اختبار أوفلاين + smoke + E2E لاعبين — كلها خضراء ضد البيئة المحلية |
| 2026-08-12 | تصحيح توثيق + حرس EngineAssociation 5.8 | 591553b.. | P6-T5 عُلّمت `[x]` (كانت مُنفّذة في 9538e85 لكن بقيت `[ ]` في PLAN.md). DEBT-1 عُلّمت `[x]` (المسارات صُحّحت + CRLF + عدّاد القادة + التحويل — الأربعة خضراء 68/82/58/59 فحصاً). verify_compile_risks L1: قائمة KNOWN قبِلت 5.8 (جهاز المطوّر) وطُفِرت إلى 6.1 بدلاً من 5.8 — الحراس 13/13 خضراء. |
| 2026-08-12 | P6-T6 الدردشة الحية في HUD — قناتا المملكة والتحالف عبر WebSocket | — | **Backend:** data/chat.json (قنوات + حدود + ثوابت واجهة) + KingdomShard: ChatMessage type + migration ver<6 (جدول chat_messages) + chatHistory في الذاكرة + snapshot + WS chat_send (تحقق: مصادقة + قناة + تحالف + حد نص + مُحدّد سرعة 5msg/5s) + WS chat_history + broadcast chat_message. **Client:** FRok2ChatMessage في Rok2Types.h + SendChat في Rok2Api (عبر WS) + OnChatMessage delegate + WS dispatch chat_message/chat_history + ParseWorld chatHistory. **Rok2ChatWidget** (جديد): فقاعات رسائل بألوان حضارات + تبويبات المملكة/التحالف + حقل إدخال + زر إرسال + تصغير/تكبير + شارة غير المقروء. **Rok2HudWidget:** زر الدردشة في BuildLeftCluster + شارة badge في الـ tick. **Rok2GameMode:** lazy-create ChatWidget عند أول ضغطة. **API.md:** توثيق chat_send/chat_message/chat_history. **PLAN.md:** P6-T6 `[x]`. |
| 2026-08-12 | P6-T7 خلفيات وأجواء القوائم — ألوان حضارية للوحات | — | **Rok2CivTheme** يكسب PanelBg/PanelBgAlt/PanelFrame — 3 ألوان جديدة لكل حضارة (روم: #1A0A0A ذهبي، الصين: #1A0D0A ذهبي إمبراطوري، العرب: #0A1A0F فيروزي، مصر: #0A1A1A ذهبي، الفايكنج: #0A0F1A رمادي، اليابان: #0D0D0A قرمزي). **BuildDefaults** يضبط الألوان الست مع الأرقام المطابقة لـ civilizations-visual-design.md. **CommanderWidget:** MainSheet/GoldFrame/InnerBg تستخدم Theme.PanelBg/PanelBgAlt/PanelFrame. **BattleReportWidget:** Rok2Panel() تأخذ Civ وتعيد Theme.PanelBg. **AllianceRosterWidget:** MainBorder يستخدم Theme.PanelBg. **verify_ui_civ_bg.mjs:** 31 فحصاً — الحراس 15/15 خضراء. |
| 2026-07-29 | P4-T5 قادة إضافيون (18) + anti-cheat أساسي | f0c3944 | data/commanders.json يكسب 6 قادة جدد (cmd_<nation>_2: Germanicus/Zhuge Liang/Saladin/Imhotep/Lagertha/Oda Nobunaga — واحد لكل حضارة، بمهارات attack/defense/passive كاملة)؛ data/anticheat.json جديد (6 rate limits: march/pass_attack/help/shop_buy/use_speedup/rally بحدود نافذة+cooldown + anomaly: سقف قوات المسيرة الكلي/المفرد، سقف المسيرات النشطة 5، سقف شراء 99)؛ sim/anticheat.ts جديد يقرأ JSON فقط (AntiCheatRateLimiter نافذة منزلقة + checkMarchPayload/checkShopBuyPayload)؛ KingdomShard: فحص شذوذ + rate limit في createMarch + سجل مخالفات (آخر 50) + GET /do/anticheat/violations؛ router.ts: enforceRateLimit على help/shop_buy/use_speedup/rally (429 rate_limited_<reason> + retryAfterMs) + GET /v1/admin/anticheat؛ اختباران: anticheat_offline_test.mjs جديد (34 فحصاً) + commanders_offline_test.mjs موسّع (19 فحصاً) — كل الاختبارات الأوفلاين الـ 13 ناجحة بدون كسر |
| 2026-07-29 | P4-T7 تحسين أداء UA (العميل) | f345f41 | URok2Perf جديد (UGameInstanceSubsystem): GetEngineMesh يخبئ /Engine/BasicShapes بعد أول تحميل (كان LoadObject متكرراً في كل ApplyArchStyleToRoof × مبنى × rebuild) + مسبح StaticMeshActor عام (Acquire/Release إخفاء/إظهار بدل Destroy/SpawnActor بسقف 64 قابل للضبط) + WorldRenderDistance موحد (كان ثابتاً سحرياً 1000000² في WorldRenderer)؛ WorldRenderer: ClearActors/SpawnMarkerActor/تنظيف المسيرات عبر المسبح + LOD من الـ subsystem + التلال تُزرع بمفتاح محتوى (ArtHillsKey) وتبقى ثابتة عبر التحديث كل 3 ثوانٍ بدل إعادة الزرع الكاملة؛ CityLayoutActor: ClearBuildings يخفي ويعيد التدوير (RecycledBuildings) بدل Destroy عند كل city upsert — حركة البناء للزرع الجديد فقط والفائض يُدمّر فعلياً؛ BuildingActor: أنماط الأسقف الستة عبر الخبأ؛ scripts/verify_perf_ua.mjs: 56 فحصاً بنيوياً ناجحاً (بناء UE5 الفعلي على جهاز المطور) |
| 2026-07-29 | P4-T6 matchmaking ممالك | 2a1b204 | data/matchmaking.json جديد (استراتيجية least_fill + round_robin احتياطية + guardrails: hard_cap_ratio=1.0 وprefer_below_ratio=0.9)؛ migration 0008 (players.kingdom_id + جدول kingdom_assignments بسجل التعيين/الاستراتيجية/نسبة الامتلاء)؛ sim/matchmaking.ts جديد يقرأ JSON فقط (fillRatio + chooseKingdom حتمي بكسر تعادل: الأقل عدداً ثم الأبجدي)؛ city/init: عند توفر ممالك إطلاق مفتوحة يُعيَّن اللاعب للأقل امتلاءً بدل افتراض مملكة البيئة + بوابة سعة لكل مملكة + تسجيل التعيين في D1 + kingdom وmatchmaking{strategy,fillRatio,reason} في الاستجابة؛ GET /v1/city يكسب kingdom؛ GET /v1/matchmaking/status عام (إشغال كل مملكة + أحدث 10 تعيينات)؛ matchmaking_offline_test.mjs جديد: 30 فحصاً — كل الاختبارات الأوفلاين الـ 12 ناجحة بدون كسر |
| 2026-07-29 | P6-T1 نظام أيقونات UI موحد (العميل) | 288006b | URok2IconLibrary جديد: رسم إجرائي لكل الأيقونات على UTexture2D (32×32 RGBA بخبأ id@size) — ~55 رمزاً (موارد food/wood/stone/gold/gems/ap، أزرار build/helmet/shield/bag/banner/map/scroll/edit/bell، حالات conn/lock/hourglass/close/star/skull/trophy/handshake/speedup، مباني wheat/box/mail/cart/horse/bow/tent/tower/castle/bricks/rock/beer/scale/crown/builder/pickaxe/monument/wrench) بأسلوب «مخطوطة ملكية» عاجي #F5E9D0 قابل للصبغ، حجم موحد 24/32/48؛ URok2ArtAssets::GetIconBrush+HasIcon واجهة ثابتة تخدم كل الواجهات؛ استبدال كل الإيموجي في 10 ودجات (Hud/BuildMenu/BuildingDetail/Commander/March/BattleReport/City/Boot/AllianceRoster/Research) — كل مورد وزر أصبح UImage أيقونة إجرائية + نص، شارة اتصال وجرس مصبوغتان ديناميكياً، توست وإشعارات Api بلا رموز تعبيرية؛ scripts/verify_ui_icons.mjs: 167 فحص بنيوي ناجح (فهرس التغطية + الربط + صفر إيموجي) |
| 2026-07-29 | P6-T3 Motion design للواجهة (العميل) | d88543f | URok2MotionLibrary جديد (Public/Rok2MotionLibrary.h + Private/Rok2MotionLibrary.cpp): محرك توين يُحدَّث على FTSTicker الأساسي — لا NativeTick في كل ودجة؛ سبب اختيار محرك توين بدل UMG Animation assets: كل ودجات العميل تُبنى إجرائياً بـ WidgetTree->ConstructWidget وليست WBP فلا توجد UWidgetAnimation لتشغيلها، والمكتبة تحرّك نفس خصائص الرندر (RenderTranslation/RenderScale/RenderOpacity/RenderTransformPivot). 14 حركة: FadeIn/FadeOut، SlideInBottom/SlideOutBottom (Bottom Sheet)، SlideInRight/SlideInLeft، ScaleInCenter/ScaleOutCenter (نوافذ تفتح من المركز بمحور 0.5,0.5 وتجاوز OutBack)، ToastIn/ToastOut، Press/Release، GoldFlash، Pulse؛ 6 منحنيات تسهيل (Linear/OutCubic/InCubic/InOutCubic/OutQuad/OutBack)؛ Rok2MotionSpec مصدر واحد للمعيار من ui-ux-design-system.md §9 (Std=0.25s ease-out) و§8.6 (Fast=0.09s رد فعل لمس <100ms) + SheetOffset/ToastOffset/SideOffset/PressScale/ScaleInFrom/FlashDuration، وقيم UFUNCTION الافتراضية حرفية لأن UHT يقرأها كنص (محروسة بـ static_assert ضد الانحراف)؛ الضغطة المحسوسة عبر URok2ButtonPressFx (وكيل UFUNCTION لأن UButton::OnPressed ديناميكية ولا تقبل lambda) — تصغير 94% + صوت ERok2AudioType::ButtonClick، مربوطة على 20 زراً في 10 ودجات، مع تنظيف دوري للوكلاء كل 10ث لأن الشبكات تُعاد بناؤها؛ الربط: BuildingDetail (انزلاق سفلي بدل انزلاق محلي في Tick — أُزيلت SlideT/bSlidIn)، BuildMenu (ورقة + تبويبات + بطاقات)، Hud (توست ينبثق من الأسفل ويخرج بحركة بدل حساب شفافية يدوي كل إطار + وميض ذهبي على الجرس عند الإشعار)، BattleReport وMarch وAllianceRoster (فتح من المركز)، Commander وBoot وCity وResearch (تلاشي دخول)؛ كل اللوحات تُسرَّح بـ PlayFadeOut بدل RemoveFromParent المفاجئ — تنفيذاً لقاعدة §1 «لا قفزات جامدة»؛ صلابة: الودجات TWeakObjectPtr فلا تعليق مؤشرات، الإسقاط من المصفوفة قبل RemoveFromParent منعاً لإعادة الدخول، إلغاء تسجيل الـ ticker في BeginDestroy، وإطار البداية يُرسم بنفس ApplyTween فلا يتكرر منطق «من أين تبدأ الحركة»؛ scripts/verify_ui_motion.mjs: 211 فحص بنيوي ناجح (تغطية الحركات + المعيار + الربط في 10 ودجات + صفر حركة محلية مرتجلة + سلامة البناء) — ولا تدهور في أي سكربت تحقق سابق (نفس عدد الأخطاء على main الأصلي) |
| 2026-07-29 | توثيق: AGENTS.md حالة الاكتمال + API.md شامل + المرحلة 6 | 51d7b18 | AGENTS.md: آخر تحديث يعكس اكتمال المراحل 1–5 والمتبقي التشغيلي + المرحلة 6 النشطة؛ API.md: توثيق endpoints الناقصة دفعة واحدة — city/init (kingdom + matchmaking في الاستجابة)، /v1/city (kingdom)، Shop+VIP (catalog/buy/use-speedup/daily-gems)، Battle Pass (status/unlock/claim + xp_sources)، Season (schedule/leaderboard/scoreboard)، Events (active)، Launch (status)، Matchmaking (status + قواعد التعيين)، Anti-cheat (حدود 429 rate_limited_* + أخطاء anticheat_* + /v1/admin/anticheat) + /v1/admin/retention؛ PLAN.md: المرحلة 6 (Polish & Identity) — 10 بنود متخصصة UI/UX + طابع بصري + قصة وأسلوب: أيقونات UI موحدة، typography عربي، motion design، FTUE، نَفَس قصصي للحضارات من data/civilizations.json، دردشة حية، خلفيات قوائم لكل حضارة، أصوات واجهة، أيقونات خريطة، شاشة حكاية المملكة |
| 2026-07-29 | إصلاح: ربط AddDynamic بمتغيّر FName — عائق بناء سابق (العميل) | 7bccef6 | العطل: 5 مواضع كانت تكتب `Btn->OnClicked.AddDynamic(this, Handler);` وHandler من نوع `const FName` يُمرَّر إلى lambda مشتركة تبني عدة أزرار — Rok2HudWidget.cpp:242 (SpawnSmall) و:282 (MakePill)، Rok2BuildMenuWidget.cpp:129 (MakeTab)، Rok2BuildingDetailWidget.cpp:264 (MakeBtn)، Rok2CommanderWidget.cpp:271 (MakeActionBtn). السبب: `AddDynamic` ماكرو يتوسّع إلى `__Internal_AddDynamic(Obj, FuncName, STATIC_FUNCTION_FNAME(TEXT(#FuncName)))` ومعامله الثاني مؤشر دالة عضو (`TMethodPtrResolver<UserClass>::FMethodPtr`) — فالعطل مضاعف: (1) خطأ ترجمة صريح لأن FName لا تتحول إلى مؤشر دالة عضو، (2) وحتى لو تُرجمت فإن `#FuncName` يحوّل *اسم المعامل* نصّياً فيصير الاسم المربوط الحرفي "Handler" لا قيمة المتغيّر — ربط خاطئ أصلاً. الإصلاح: هيدر جديد `Public/Rok2DelegateBind.h` فيه `Rok2BindClickByName(UButton*, UObject*, FName)` يستخدم مسار الربط بالاسم العام في المحرك — `FScriptDelegate::BindUFunction` ثم `OnClicked.Add` (صالح لأن FOnButtonClickedEvent يرث TMulticastScriptDelegate<FWeakObjectPtr> وفيه Add(const FScriptDelegate&) عامّة، وFScriptDelegate هو TScriptDelegate<FWeakObjectPtr> وفيه BindUFunction عامّة) — بلا تبعية موديول جديدة وبلا تعديل Rok2.Build.cs. لماذا دالة مساعدة واحدة بدل تكرار ثلاث أسطر في كل موضع: الربط بالاسم يُحلّ في وقت التشغيل، فلو الاسم غير موجود أو غير UFUNCTION يفشل الربط **بصمت** ويصير الزر ميتاً بلا أي خطأ — فالدالة تتحقق بـ `FindFunction` وتصرخ بـ `ensureMsgf` في بناء التطوير، وتحرس المعاملات ضد null وNAME_None. المواضع الـ53 الأخرى من AddDynamic تمرّر `&Class::Method` صحيحاً ولم تُلمس. ملاحظة عن الجذر: 4 من 5 مواضع تسبق P6-T1، أي أن الوحدة لم تُبنَ فعلياً على UE5 من قبل. `scripts/verify_delegate_bind.mjs`: 64 فحص ناجح — يحرس صنف الخطأ في كل الموديول (أي AddDynamic/AddUniqueDynamic معامله الثاني ليس `&`)، ويؤكد أن الأسماء الـ16 كلها تُحلّ إلى `UFUNCTION()` بتوقيع `void()` في هيدر صنفها (Hud 7، BuildMenu 3، BuildingDetail 3، Commander 3) — وهو الفحص الذي يمنع عودة العطل الصامت. اختبارَا نفي يثبتان أن السكربت ذو أسنان: على كود main الأصلي يفشل بـ15 خطأ ويحدّد المواضع الخمسة بأرقام أسطرها، وعند إعادة تسمية مُعالج في الهيدر وحده يرصد الاسم غير القابل للحل. لا تدهور: كل سكربتات التحقق الثمانية السابقة بنفس نتائجها بالضبط (civ_themes 0/9، commanders_screen 0/6، fog_of_war 0/8، game_feel 0/7، perf_ua 56/0، produced_assets 138/11، ui_icons 167/0، ui_motion 211/0). **حدّ التحقق:** لا مترجم C++ في بيئة الجلسة، فالتحقق بنيوي واستدلال على API المحرك فقط — بناء UE5 الفعلي ما زال لازماً على جهاز المطور لتأكيد اختفاء العائق. |
| 2026-07-29 | P6-T2 Typography عربي/لاتيني متوازن (العميل) | f00636c, 046f6a2, 9b451b1, d62698e, b2ce631 | **`URok2Typography` جديد** (Public/Rok2Typography.h + Private/Rok2Typography.cpp): 3 أوجه من وثيقة UI §1 — `Display` (Aref Ruqaa، العناوين الكبرى) و`Ui` (Cairo، معظم الواجهة) و`Numeric` (Cinzel، الأرقام والمؤقّتات) — و**12 دوراً نصياً** (Display/Title/Subtitle/TitleCompact/CardTitle/Body/BodySmall/Button/Caption/Micro/Numeric/Timer)، كل دور يحمل وجهاً وحجماً ووزناً في مكان واحد فتطلب الودجات **دوراً لا رقماً**. **السلم مُقاس لا مُختَرَع:** مسحي الأول (`.Size = N`) فوّت HudWidget كلياً لأنه يمرّ عبر مساعد محلي، وفوّت CommanderWidget لأنه ينادي `FCoreStyle` مباشرة — فظهرت 4 أنماط لضبط الخط لا نمط واحد، والمدى الحقيقي 8..28 لا 12..24. الدرجات الثماني (11/12/14/15/16/18/22/24) تستوعب 25 من 32 موضعاً بدقة تامة، والانحرافات مقصودة وموثّقة: 8→11 (لافتة تحت زر قطره 58px بأيقونة 26px — و8px عربي غير مقروء على الهاتف)، و20→18 و28→24 **توحيد** يجعل عناوين الأقسام واحدة وعناوين الشاشات واحدة عبر كل الشاشات. **الخط لا يحمل لوناً:** `ApplyFont` تمسّ الخط وحده ولا تلمس `SetColorAndOpacity`، فالألوان الديناميكية (شارة الاتصال، الجرس، تعذّر التكلفة) تبقى لكل ودجت — يحرسه المدقق T7. **تدهور لطيف** على اصطلاح `URok2ArtAssets` مع GLB: `LoadObject<UFont>` من `/Game/Fonts/<name>.<name>` وإلا `FCoreStyle` بنفس الحجم والوزن، بخبأ للفشل فلا تتكرر المحاولة، وقصر الأوزان غير المضمونة على خط المحرك (`Black`⇒`Bold`) لأن طلب Typeface غائب يجعل Slate يقع على أول وجه فيضيع التمييز **بصمت**؛ و`Content/Fonts/README.md` يشرح الاستيراد. **رُحّل 63 موضعاً في 9 ودجات:** Hud (15، وفقد مساعده المحلي `Rok2Font`)، Commander (20)، BuildingDetail (6)، BattleReport (6، ولامدا `AddLine` تأخذ دوراً بدل رقم)، Boot (5)، City (3، وأرقام الموارد الأربعة كانت ترث خط النص الوصفي نفسه)، BuildMenu (3)، MarchPanel (3)، AllianceRoster (2). ومن ذلك إصلاح ضمني: `->Font.Size = N` في AllianceRoster وMarchPanel (5 مواضع) كانت تكتب على العضو مباشرة فتتجاوز `SetFont` وإبطالها. **قرارات تصميم:** أُضيف `BodySmall` و`CardTitle` لأن الترحيل أثبت الحاجة (لا استباقاً)، وأُعيدت تسمية `HudTitle`→`TitleCompact` قبل أن ينتشر لأن الاسم كان سيضلّل في صفوف القوائم، و`FontSized` منفذ للأحجام المحسوبة (حرف البورتريه = نصف القطر) ليبقى الوجه من النظام، و**حُذف `NumericLarge`** لأنه بلا مستدعٍ — دور لا يستخدمه أحد يتعفّن، ويحرس ذلك المدقق T8. **`verify_ui_typography.mjs`: 47 فحصاً + 13 اختبار نفي.** الطور الثاني كشف حرسين واهيين عندي فأُصلحا: T6 كان يفحص وجود مسار الرجوع على مستوى الملف فبقي ناجحاً بعد حذفه من `Font()` لأن `FontSized` تحتوي نداءً مماثلاً (قُصر على جسم `Font` نفسها)، وطفرة T10 أصابت `FaceOf` لا `WeightOf` لأن نص المرساة يظهر في الاثنين — فأُضيف `replaceUnique` يرفض أي مرساة ملتبسة، إذ اختبار نفي يعدّل الموضع الخطأ يعطي طمأنينة كاذبة تماماً كفحص بتعبير نمطي خاطئ. **عدم التدهور:** أعداد الأخطاء مطابقة للأساس في السكربتات التسعة كلها. **تحفّظ:** لا مترجم C++ ولا عين على اللعبة — التحقق بنيوي، ولا ادّعاء ببناء ناجح ولا بالشكل النهائي على الشاشة. |
| 2026-07-29 | إغلاق تدقيق مخاطر الترجمة: حرس بنيوي + نتائج LOW | 6df5230, ad7d95c, c9c6bc3, e62ec3c | **`scripts/verify_compile_risks.mjs` (e62ec3c) — حرس بطورين:** الطور 1 يشغّل 15 مدققاً على الشجرة الحقيقية (49 فحصاً، كلها ناجحة)؛ والطور 2 **اختبار نفي**: لكل مدقق يُعاد زرع عطله الأصلي في نسخة VFS في الذاكرة ويُشترط أن يفشل المدقق هناك — حرسٌ لا يفشل على العطل لا يُثبت شيئاً، ومدقق ينجح في الطور 1 ولا يفشل في الطور 2 يُحتسب عطلاً في الحرس نفسه. تحققتُ أن كل اختبار نفي يفشل **للسبب المقصود** لا عرَضاً (طُبع الفحص الفاشل لكل طفرة). التغطية: الـ11 مجموعة (حجب OnClicked، الإعلان المزدوج، FJsonObject، انتهاكا protected، Rok2SendRequest، BuildSettingsVersion، USpacer، ترتيب FRok2ZoneStatus، تضمين Rok2Types.h، تضمينات المحرك في .cpp، UPROPERTY خامد) + نتيجتَي LOW + اكتفاء الهيدرات + سلامة أداة الفحص. **نتيجة LOW الأولى (6df5230):** `EngineAssociation: "5.8"` نسخة غير موجودة — وُحّدت على **5.4** (أرضية `UNREAL_ENGINE_GUIDE.md` وقرار §29 «≥5.4 والكود يبني على أيها») في `Rok2.uproject` + 3 وثائق + 3 سكربتات مطوّر (`UE_ROOT`/`UE_PATH` صارت قابلة للتجاوز من البيئة). هذا يُكمل ما ادّعاه P1-T1: كان `README.md` يناقض نفسه (5.8 في سطر و5.4 في آخر). ملفا `.slnx` تُركا عمداً (يعيد Visual Studio توليدهما، و60+ مساراً مولّداً لا تمسّ قابلية البناء). **نتيجة LOW الثانية (ad7d95c):** UMG/Slate/SlateCore/HTTP/WebSockets/Json كانت `Private` وتظهر في هيدرات عامة — نُقلت إلى `Public` بعد تحقّق بالـgrep (UserWidget.h+Button.h في 14 هيدر، SlateBrush.h في هيدرين، IHttpRequest/IHttpResponse وIWebSocket وFJsonObject في تواقيع Rok2Api.h/Rok2CivThemes.h)؛ وبقيت JsonUtilities/NavigationSystem/AIModule/RenderCore/RHI خاصة (لا يذكرها أي هيدر عام)، والاتحاد محفوظ بلا فقدان وحدة. **إصلاحان خارج قائمة التدقيق (c9c6bc3):** (أ) 19 إشارة نوع محرك في 11 هيدراً عاماً كانت تعتمد على **تضمين عابر** — أُضيفت تصريحات أمامية بعد التحقق أن كل استخدام بمؤشر فقط (فالتصريح كافٍ وآمن). (ب) عطل كامن في `stripComments` بـ `verify_delegate_bind.mjs`: كان يزيل تعليقات **الكتل قبل السطر**، فأي تعليق سطر يحتوي فاتحة كتلة (كالمسار `/Engine/BasicShapes/` متبوعاً بنجمة في `Rok2Perf.h:4`) يفتح كتلة وهمية تُغلق عند أول خاتمة بعده فتُمحى عشرات أسطر الكود الحقيقي ويجري الفحص على فراغ — **نجاح كاذب صامت**. وقعتُ في العطل نفسه أثناء العمل: مسحي للهيدرات أبلغ عن إشارتين «غير محلولتين» كانتا مُصرَّحتين فعلاً. الترتيب صار السطر أولاً، ويحرسه المدقق S0 باختبار نفي، و64/64 كما هي. **عدم التدهور:** قِيس مقابل الأساس الحقيقي `f94c2bb` — أعداد الأخطاء متطابقة في كل السكربتات التسعة (civ_themes 10، commanders 7، fog 9، game_feel 8، produced_assets 12، وperf_ua/ui_icons/ui_motion/delegate_bind صفر). **تحفّظ:** لا مترجم C++ في البيئة — التحقق بنيوي، ولا ادّعاء ببناء ناجح. |
| 2026-07-29 | تدقيق مخاطر الترجمة في وحدة Rok2 (صنف عطل AddDynamic) | dd8fb08 | ثلاثة مدققين مستقلين على 14,477 سطراً/68 ملفاً، وكل نتيجة عالية الثقة تحققتُ منها بنفسي. **عوائق مؤكدة أُصلحت:** (1) `ARok2BuildingActor::OnClicked` كانت دالة عضو تحجب خاصية الـdelegate `AActor::OnClicked` — حجب الأسماء يُخرج عضو الأساس من مجموعة البحث كلياً فيصير `OnClicked.AddDynamic(...)` تطبيقاً لـ`.` على اسم دالة؛ كسر `Rok2BuildingActor.cpp:64` و`Rok2CityLayoutActor.cpp:231` → أُعيدت التسمية إلى `HandleActorClicked`. هذه بالضبط نقطة توقف الجلسة السابقة (تعارض أسماء الأصناف الأساسية) ووُجد العطل فيها فعلاً. (2) `OnCloseClicked` معلنة مرتين في `Rok2BuildMenuWidget.h` (protected+private) وتعريف واحد → حُذف الإعلان الشارد. (3) `FJsonObject` غير معلن في `Rok2Api.h` (9 مواضع) و`Rok2CivThemes.h` ولا تصريح أمامي له في الوحدة، وهو نوع غير منعكس فلا يولّده UHT → تصريح أمامي في الهيدرين. (4) `Rok2CityLayoutActor` كان يصل إلى `bUsingArtAsset`/`ApplyCivTheme()` المحميين من صنف شقيق → نقطة دخول عامة `MarkUsingArtAsset()`. (5) الدالة الحرة `Rok2SendRequest` تصل إلى 5 أعضاء محمية بلا friend → صارت عضواً ساكناً محمياً. (6) `BuildSettingsVersion.V7` لا وجود له (التعداد V1–V5+Latest) في ملفَي Target — وUBT يترجمها C# **قبل** أي C++ فهو توقف تام → `Latest`. (7) `USpacer` بلا `Components/Spacer.h` في `Rok2BuildMenuWidget.cpp` (الملفات الخمسة الأخرى تضمّنه). (8) `FRok2ZoneStatus` كانت تُستخدم في `:242` وتُعلن في `:432` → نُقلت فوق مستهلكها. (9) 4 هيدرات تسمّي بنى `FRok2*` بلا `Rok2Types.h`. (10) تضمينات ناقصة: MaterialInstanceDynamic×3، StaticMesh×5، SlateColorBrush×1. (11) `UPROPERTY` خامد داخل بنية غير USTRUCT (`FToastEntry`) يوهم بتتبّع GC غير موجود → أُزيل ووُثّق أن المرساة `ToastCardRefs`. **إيجابيات كاذبة رُفضت:** صفر قيمة افتراضية غير قانونية في 22 حالة، صفر عدم تطابق في 53 موضع ربط مقابل 21 تصريحاً، صفر وحدة ناقصة في Build.cs، 97 تصريحاً أمامياً كلها صحيحة، و`FHttpRequestPtr` (نقطة توقف الجلسة السابقة الأخرى) سليم تماماً. **تحفّظ:** لا مترجم C++ في البيئة — التحقق بنيوي واستدلال على API المحرك، ولا ادّعاء ببناء ناجح. **أُغلق كله في e62ec3c وما قبله** — انظر الصف التالي. |
| 2026-07-29 | P6-T4 Onboarding أول دقيقة (FTUE) (العميل) | ff7d462, ae8f249, 78b5de4, 3850206, 9c95d88 | **`URok2Onboarding` (نموذج) + `URok2OnboardingWidget` (طبقة)** — الفصل مقصود: منطق «أين اللاعب الآن؟» قابل للفحص وحده بلا ودجة ولا شاشة. ثلاث خطوات من المواصفة (مزرعة ← جنود ← مسيرة جمع)، كل خطوة تحمل عنواناً أدبياً وسطر حكاية وسطر إجراء **منفصلين** («رغيفٌ قبل السيف» / «لا تُبنى مملكةٌ على جوع…» / «المس المطرقة، ثم اختر المزرعة») — الفصل لأن جملة أدبية وحدها تترك اللاعب لا يعرف أين يضغط، وتعليمة جافة وحدها تُلغي الطابع؛ فالبطاقة تحملهما بوزنين بصريين (Body وMicro). **القرار المركزي — الخطوة تُستنتج ولا تُخزَّن:** لا `USaveGame` ولا `GConfig` في الموديول كله، والحقائق الثلاث موجودة عند الخادم أصلاً وتصل عبر مفوَّضات قائمة، فراية محلية = مصدر حقيقة ثانٍ يخالف AGENTS.md §3 «الخادم هو السلطة» ويفتح عطلاً جديداً (الراية تقول «انتهى» والمدينة خالية). والاستنتاج يشتري سلوكاً بالمجان: الإرشاد يستأنف صحيحاً بعد قتل التطبيق في منتصف خطوة، وبعد إعادة التثبيت، وعلى جهاز ثانٍ. المُسنَدات: `HasFarm` (مبنية **أو في الطابور** — الإصرار على اكتمال البناء يعني بطاقةً تُلحّ بطلبٍ نُفِّذ)، `HasTroops` (أي وحدة أو أي طابور تدريب لا المشاة وحدهم)، `HasOwnMarch` (أي مسيرة للاعب — حصرها على `node` كان سيترك من أطلقها على برابرة عالقاً في بطاقة لا تُغلق، والهدف التربوي أن يُطلق مسيرته الأولى). **الثغرة والمِزلاج:** المُسنَد الصافي **يرتدّ** — محاربٌ قديم فقد جنوده في معركة يُعرض عليه «درّب أول جنودك» من جديد، وهو استصغار للاعب لا ميزة. فالتقدّم أحادي الاتجاه بمقارنة `Ordinal` (التعداد مُرقَّم صراحةً لهذا)، و`Done` نهائية، والتسليح يحدث **مرة واحدة** فمن أنجز الثلاثة سلفاً لا يُسلَّح ولا تُبنى له الطبقة. **والفخّ الأهم مسدود:** لا تقييم قبل `IsStateReady` — مدينة قبل وصول البيانات تُشبه لاعباً جديداً تماماً فالتقييم عليها كان سيُسلّح الإرشاد لمحاربٍ قديم؛ والدليل `City.UpdatedAt` غير الصفري (يملؤه `ParseCity` من `updated_at`) لا `HallLevel` لأن افتراضيه 1 فمدينة فارغة تُشبه مستوى 1. مُحقَّق منطقياً بمحاكاة المِزلاج قبل الكتابة: **لا ارتداد في 1536 انتقالاً** عبر كل التتابعات الممكنة، وحالتا اللاعب العائد والمدينة الفارغة مثبتتان. **موضع البطاقة مقيس من تخطيط الـHUD لا مُختار بالذوق:** الشاشة مشغولة في ثلاث جهات — الشريط العلوي عرضٌ كامل×46، وأعلى يمين الطوابير والإشعارات (أنكر 1,0)، وأسفل يمين عنقود الأزرار (أنكر 1,1) **وهو هدف الخطوة الأولى**، وأسفل ورقة البناء عرضٌ كامل×340 بخلفية معتمة. فالبطاقة أعلى اليسار تحت الشريط (16,58) وهي نفسها «اليسار — المهام» في وثيقة UI §3.5؛ وأي موضع سفلي كان سيُدفن تحت ورقة البناء — وهي الورقة التي تُفتح في الخطوة الأولى بالضبط، أي أن البطاقة كانت ستختفي في اللحظة التي تُرشد فيها. **الحلقة الذهبية (تنفيذ §3.5 «يبرز الهدف بإطار ذهبي نابض»):** إطار **مفرَّغ** من أربعة أشرطة لا مستطيل ممتلئ — الممتلئ كان سيغطّي الزر أي يُبرزه بحجبه؛ وحاوية **واحدة** لا أربعة أشرطة مستقلة لأن `Pulse` حركة **مقياس** (1.0→1.08) بمحور مركزي، فأشرطة منفصلة كان كل شريط يتقيس حول مركزه فيتشوّه الإطار؛ و`Pulse` **لقطة واحدة** (0.40s) لا حلقة فتُعاد كل 1.1s وإلا صار الإطار ساكناً لا نابضاً. **والطبقة كلها `HitTestInvisible`** — حلقة تلتقط اللمس تجعل الزر الذي تشير إليه غير قابل للضغط، أي درسٌ يمنع تنفيذ نفسه؛ ولا زرّ «تخطٍّ» لأن المواصفة لا تطلبه والبديل اللطيف أن الطبقة غير حاجبة أصلاً. **سجلّ مراسٍ** بـ`TWeakObjectPtr` (شبكات الـHUD تُعاد بناؤها فمؤشر خام يتدلّى) يُبرز زرَّ خطوةٍ يسكن ودجةً أخرى بلا نشر علم الإرشاد في عشر ودجات — 3 مراسٍ فقط: دائرة البناء المرئية (96×96) لا `BuildBtn` محتواها، وزر التدريب، وحبّة الخريطة (و`MakePill` صارت تعيد `Pill` لأن الأزرار الثلاثة تُبنى بنفس اللامدا فبلا قيمة راجعة لا سبيل لتمييز أحدها). وهدف الخطوة الثالثة عقدة موارد **في العالم** لا ودجة فلا زرّ يُحاط به — نُبرز باب الخريطة والبطاقة تحمل بقية الإرشاد، وهو أصدق من ادّعاء إبراز ما لا تستطيع الواجهة الإشارة إليه. الترتيب 60: Boot 100 > الإرشاد 60 > اللوحات 50 > HUD 20 > المدينة 10. والتقدّم من المفوَّضات القائمة لا `Tick` (`OnCityLoaded` للمباني والطوابير، و`OnWorldSnapshot` يُبثّ من `UpsertMarch` **لحظة** إنشاء المسيرة فلا سباق مع انتهائها داخل الجلسة)، وحرس `LastRenderedStep` يمنع إعادة الانزلاق مع كل نبضة شبكة. والتتويج لحظة لا إخفاء صامت (theme-and-values §5): وميض ذهبي ثم تلاشٍ بمؤقّت. **عطلان أُصلحا قبل الرفع:** `SetAutoSize(true)` على شريحة الكانفس يُلغي `SetSize` تماماً فعرض 340 كان وهماً، و`SetAutoWrapText` لا يلتفّ بلا حدٍّ أفقي فالحكاية العربية كانت ستمتدّ سطراً يخرج من الشاشة — `SizeBox` بـ`WidthOverride` يحلّ الاثنين. واللافتة بدور `Caption` لا `Numeric` لأنها تخلط رقماً بكلمة «من» والوجه الرقمي (Cinzel) بلا محارف عربية فكانت الكلمة ستسقط بصمت. **`scripts/verify_ui_ftue.mjs`: 83 فحصاً + 32 اختبار نفي.** الطور الثاني كشف حرساً واهياً عندي فأُصلح: F1 كان يعدّ نداءات `MakeInfo` الثلاثة، فطفرة تُعيد تسمية **حالة** خطوة تركت النداءات قائمة فمرّ العدّ — أي أن خطوةً تفقد حالتها فتسقط إلى `default` وتعرض بطاقة فارغة كانت ستمرّ بلا رصد؛ صار الفحص يؤكد أن كل قيمة في التعداد لها حالة صريحة (العدّ كان يقيس الشيء الخطأ: العدد لا الربط). وتحققت أن **كل** اختبار نفي يفشل للسبب المقصود لا عرَضاً بطباعة الفحص المخفق لكل طفرة — 32/32 مطابقة للقصد. وF13 يقيس **التطابق** مع `data/buildings.json` لا النص: طفرته الثانية تغيّر ملف البيانات نفسه فيرصد الحرس انحراف الكود عن البيانات. **عدم التدهور مقيس لا مُدَّعى:** قِيس مقابل الأساس الحقيقي `b319b32` — أعداد الأخطاء **متطابقة** في السكربتات الأحد عشر كلها (civ_themes 0/9، commanders_screen 0/6، fog_of_war 0/8، game_feel 0/7 — أربعتها DEBT-1 قديمة؛ perf_ua 56/0، produced_assets 138/11، ui_icons 167/0، ui_motion 211/0، delegate_bind 64/0)، وكسب حرسان فحصاً لأنهما اكتشفا الملفات الجديدة تلقائياً (compile_risks 49→50، typography 47→48) — أي أن الحرس القائم **يغطّي** الجديد لا يتحمّله فقط. **تحفّظ:** لا مترجم C++ ولا عين على اللعبة — التحقق بنيوي، ولا ادّعاء ببناء ناجح ولا بالشكل النهائي على الشاشة. |
---

## 5. ملفات مرجعية سريعة

- `README.md` — مدخل المشروع
- `INDEX.md` — فهرس كل الوثائق
- `07-game-design/GDD.md` — **وثيقة التصميم الشاملة (مصدر الحقيقة للتصميم)**
- `07-game-design/castle-hex-city.md` — مواصفة القلعة السداسية والبناء الحر
- `07-game-design/civilizations-visual-design.md` — التصميم البصري للحضارات الست
- `07-game-design/rok-features-audit.md` — توثيق مزايا RoK الأصلية (المرجع)
- `07-game-design/ui-ux-design-system.md` — نظام الواجهات بأسلوب اللعبة
- `07-game-design/power-balance-map.md` — خريطة توازن القوى والجيم بلاي
- `07-game-design/assets/` — 11 صورة مرجعية مولّدة (قلعة، حضارات، وحدات، قادة، UI)
- `UNREAL_ENGINE_GUIDE.md` — تشغيل وبناء عميل UE5
- `game/docs/API.md` — مرجع الـ REST/WebSocket API
- `game/docs/RUN.md` — تشغيل ونشر الـ backend
- `data/*.json` — بيانات التوازن الموحدة
- `06-implementation/roadmap.md` — الرؤية التاريخية (تُقرأ للسياق، والمرجع النهائي هو هذا الملف)
| 2026-08-12 | P6-T8 الإحساس الصوتي + P6-T9 أيقونات العالم + P6-T10 حكاية المملكة | — | **P6-T8:** أضيفت 4 مؤثرات WAV للضغط/الفتح/الإغلاق/الخطأ و6 همسات قصيرة مميزة للحضارات، مع فهرس معرفات ومسارات في `URok2ArtAssets` و`verify_ui_sfx.mjs`. **P6-T9:** أضيفت `FRok2WorldIconStyle` و`URok2WorldIconography` كمكتبة مستقلة ترجع رمزاً دلالياً ولون مستوى وحجماً لأنواع موارد/برابرة/بوابات/عرش، و`verify_world_iconography.mjs` يطابق القواعد مع `data/zones.json`. **P6-T10:** أضيف سجل `season_story_events` في `KingdomShard` لمعالم فتح المناطق والاحتلال الأول والتحول والتحالفات والعرش وتتويج واحد حتمي، مع تضمينه في اللقطة وبث أحداث عامة؛ وأضيفت `URok2SeasonStoryWidget` لخط زمني وتُوّج الفائز، و`verify_season_story.mjs`. اجتازت فحوصات العقود الثلاثة. |
| 2026-08-12 | P7-T0 تدقيق خط الأساس وتكامل المخرجات | — | اعتُمدت المرحلة السابعة رسمياً. أضيف `game/docs/P7_T0_BASELINE_AUDIT.md` و`game/client-unreal/scripts/verify_p7_baseline.mjs` وربط `test:p7-baseline` ببوابة الجودة. التدقيق يثبت أصول P6 ويصنّف فجوات الربط INT-01 (الصوت عبر `URok2ArtAssets`)، INT-02 (أيقونات العالم عبر `URok2WorldIconography`)، وINT-03 (حكاية المملكة بين `KingdomShard` وAPI/HUD) على أنها جاهزة للمكوّنات لكن غير موصولة في التشغيل؛ ملكيتها ومعيار إثبات PIE موثقان لـ P7-T1. |
| 2026-08-12 | P7-T1 توصيل هوية P6 في التشغيل — WIP | — | **INT-01:** سجل `Rok2AudioManager` أصول P6 ويشغل نقرة موحدة وفتح/إغلاق اللوحات وخطأ WS وهمس الحضارة. **INT-02:** يستهلك `Rok2WorldRenderer` `URok2WorldIconography` لتلوين/تحجيم/وسم الممرات والعرش والعقد. **INT-03:** يحلل `Rok2Api` لقطة `seasonStory` ويعالج الحدث الحي؛ يملك `Rok2GameMode` إنشاءً كسولاً للودجة وتعبئةً من اللقطة وزر HUD. أضيف `verify_p7_t1_integration.mjs` إلى `npm run check`؛ البوابة اجتازت كاملة. لا تكتمل المهمة حتى يوثق PIE على UE 5.4+ لأن المحرر غير متوفر في البيئة. |
